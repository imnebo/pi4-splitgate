---
phase: "12-buffered-asn-output"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/watch-routes.py
  - src/tests/test_watch_routes_asn.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "watch-routes.py never prints a line for a new destination IP until its ASN lookup completes or BUFFER_TIMEOUT (6.0s) elapses"
    - "IPs already in _asn_cache (resolved or explicitly empty) print immediately — no added latency on repeated IPs"
    - "A daemon watchdog thread runs every 1s and flushes any pending entry older than BUFFER_TIMEOUT"
    - "_do_lookup() flushes pending entries for its IP immediately after writing _asn_cache, before the watchdog fires"
    - "--no-asn mode is unaffected: lines print immediately with no buffering"
    - "format_line() signature and return type are unchanged — buffering logic lives in main()"
  artifacts:
    - path: "src/scripts/watch-routes.py"
      provides: "Buffered output: new IPs held until ASN resolves"
      contains: "_pending"
    - path: "src/scripts/watch-routes.py"
      contains: "_BUFFER_TIMEOUT"
    - path: "src/scripts/watch-routes.py"
      contains: "_pending_watchdog"
    - path: "src/tests/test_watch_routes_asn.py"
      provides: "Updated tests covering buffer + flush behavior"
---

<objective>
Extend `src/scripts/watch-routes.py` to buffer log lines for new destination IPs until their ASN/org lookup completes, so every printed line shows full enrichment data. Previous behavior: first-seen IPs printed immediately without `| org` suffix; the suffix only appeared on a subsequent packet for the same IP. New behavior: first-seen IPs are held in a pending buffer; once `_do_lookup()` completes (typically 1–3 s) the line is flushed with the resolved org. If the lookup stalls, a watchdog thread flushes after `_BUFFER_TIMEOUT = 6.0` seconds.

No changes to deploy.sh, asn-lookup.py, vpn-status.sh, or routing.sh — pure watch-routes.py UX change.
</objective>

<verification>
- `python3 -m pytest src/tests/test_watch_routes_asn.py -v` → 17 passed (9 original + 7 new B-series + 1 help test)
- `python3 -c "import ast; ast.parse(open('src/scripts/watch-routes.py').read())"` → exit 0
- `python3 src/scripts/watch-routes.py --help` → exits 0, no new flags
- Live (on RPi): new destination IPs appear after ~1–3 s with `| org` suffix
</verification>

<success_criteria>
1. src/scripts/watch-routes.py: _pending buffer + _pending_lock + _BUFFER_TIMEOUT added
2. src/scripts/watch-routes.py: _flush_entries() flushes buffered lines with asn_result
3. src/scripts/watch-routes.py: _do_lookup() flushes pending after cache update
4. src/scripts/watch-routes.py: _pending_watchdog() daemon thread flushes timed-out entries
5. src/scripts/watch-routes.py: main() buffers new/in-flight IPs; cached IPs print immediately
6. src/scripts/watch-routes.py: --no-asn path unchanged (no buffering)
7. src/tests/test_watch_routes_asn.py: 17 total tests pass (10 existing + 7 new)
8. format_line() signature unchanged
</success_criteria>

<output>
✓ Complete — 2026-05-28
</output>
