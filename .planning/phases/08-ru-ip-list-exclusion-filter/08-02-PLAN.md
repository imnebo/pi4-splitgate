---
phase: 08-ru-ip-list-exclusion-filter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "deploy.sh declares EXCLUDE_LIST_LOCAL, EXCLUDE_LIST_REMOTE, EXCLUDE_LIST_TMP path variables"
    - "After Stage 21 (white-list-extended.txt), deploy.sh conditionally SCPs configs/ru-exclude.txt to /etc/ru-exclude.txt when the local file exists"
    - "If configs/ru-exclude.txt does not exist locally, Stage 21 block skips the exclude file silently"
    - "TOTAL_STAGES is not changed (remains 24)"
    - "Final Summary section prints a PHASE 8 line for the deployed exclude file"
  artifacts:
    - path: "deploy.sh"
      provides: "EXCLUDE_LIST_* path vars + conditional SCP block + PHASE 8 summary line"
      contains: "EXCLUDE_LIST_LOCAL"
  key_links:
    - from: "EXCLUDE_LIST_LOCAL variable"
      to: "SCP conditional block"
      via: "[[ -f \"${EXCLUDE_LIST_LOCAL}\" ]] guard"
      pattern: "EXCLUDE_LIST_LOCAL"
---

<objective>
Extend deploy.sh with three changes:
(A) Add EXCLUDE_LIST_* path variable declarations in the path variable block.
(B) Add a conditional SCP block after Stage 21 that deploys configs/ru-exclude.txt to
    /etc/ru-exclude.txt on the RPi when the local file exists.
(C) Add a PHASE 8 echo line in the Final Summary section.

No TOTAL_STAGES bump — the new conditional is adjacent to Stage 21, not a new numbered stage.

Purpose: Operators who create configs/ru-exclude.txt locally will have it deployed to the RPi
automatically on the next ./deploy.sh run, matching the Stage 21 pattern for white-list-extended.txt.

Output: Modified deploy.sh with three localized additions.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/ROADMAP.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/phases/08-ru-ip-list-exclusion-filter/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add path variable declarations to deploy.sh</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh lines 62–68 — the end of the path variable block. The last three lines are:
        WHITE_LIST_EXT_LOCAL="configs/white-list-extended.txt"
        WHITE_LIST_EXT_REMOTE="/etc/white-list-extended.txt"
        WHITE_LIST_EXT_TMP="/tmp/white-list-extended.tmp"
        NM_DISPATCHER_LOCAL="scripts/10-vpn-routes"
        NM_DISPATCHER_REMOTE="/etc/NetworkManager/dispatcher.d/10-vpn-routes"
        NM_DISPATCHER_TMP="/tmp/10-vpn-routes.tmp"
      Insert the three new vars after WHITE_LIST_EXT_TMP and before NM_DISPATCHER_LOCAL.
  </read_first>
  <action>
Insert exactly these three lines after the `WHITE_LIST_EXT_TMP="/tmp/white-list-extended.tmp"` line
and before the `NM_DISPATCHER_LOCAL=` line (per D-07, naming matches the white-list pattern):

```
EXCLUDE_LIST_LOCAL="configs/ru-exclude.txt"
EXCLUDE_LIST_REMOTE="/etc/ru-exclude.txt"
EXCLUDE_LIST_TMP="/tmp/ru-exclude.tmp"
```

No other changes to this block.
  </action>
  <verify>
    <automated>
      grep -n "EXCLUDE_LIST_LOCAL\|EXCLUDE_LIST_REMOTE\|EXCLUDE_LIST_TMP" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh
      bash -n /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh
    </automated>
  </verify>
  <done>
    - grep returns exactly 3 lines for the three EXCLUDE_LIST_* variables
    - EXCLUDE_LIST_LOCAL="configs/ru-exclude.txt" is present
    - EXCLUDE_LIST_REMOTE="/etc/ru-exclude.txt" is present
    - EXCLUDE_LIST_TMP="/tmp/ru-exclude.tmp" is present
    - bash -n passes with no syntax errors
    - TOTAL_STAGES remains 24 (grep "TOTAL_STAGES=24" returns 1 line)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Stage 21b conditional SCP block and PHASE 8 summary line</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh lines 361–392 — Stage 21 block through the start of the Final Summary.
      Stage 21 ends at the closing `fi` on line 369. Stage 22 starts on line 371.
      The Final Summary starts at line 389. PHASE 7 summary line is on line 413 (the
      `echo "   PHASE 7: ${ASN_LOOKUP_REMOTE}..."` line).
  </read_first>
  <action>
Change A — After the Stage 21 `fi` (which closes the white-list-extended.txt conditional)
and before the `# ─── Stage 22:` comment line, insert the following block. Use the same
comment-bar style as all other Stage comments:

```
# ─── Stage 21b: Deploy ru-exclude.txt to RPi (D-07, D-08) ──────────────────
echo "[21/${TOTAL_STAGES}] Deploying ru-exclude.txt to ${SSH_HOST} (if present)..."
if [[ -f "${EXCLUDE_LIST_LOCAL}" ]]; then
    scp -o BatchMode=yes "${EXCLUDE_LIST_LOCAL}" "${SSH_HOST}:${EXCLUDE_LIST_TMP}"
    ssh -o BatchMode=yes "${SSH_HOST}" "sudo mv ${EXCLUDE_LIST_TMP} ${EXCLUDE_LIST_REMOTE} && sudo chmod 644 ${EXCLUDE_LIST_REMOTE} && sudo chown root:root ${EXCLUDE_LIST_REMOTE}"
    echo "       ru-exclude.txt deployed (mode 644, root:root)."
else
    echo "       ${EXCLUDE_LIST_LOCAL} not found in repo — skipping exclude list deploy (D-04)."
fi
```

Note: The echo uses [21/${TOTAL_STAGES}] — same stage number as Stage 21, not a new stage
(per D-08: no TOTAL_STAGES bump).

Change B — In the Final Summary section, after the existing PHASE 7 echo line
(`echo "   PHASE 7: ${ASN_LOOKUP_REMOTE}..."`), insert:

```
echo "   PHASE 8: ${EXCLUDE_LIST_REMOTE} (mode 644, root:root, optional — deployed only if ${EXCLUDE_LIST_LOCAL} exists)"
```

Insert this as a standalone `echo` line, on its own line, following the PHASE 7 line.
  </action>
  <verify>
    <automated>
      grep -n "Stage 21b\|ru-exclude\|EXCLUDE_LIST" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh
      grep -n "PHASE 8" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh
      grep -c "TOTAL_STAGES=24" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh
      bash -n /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh
    </automated>
  </verify>
  <done>
    - bash -n passes (no syntax errors)
    - grep "Stage 21b" returns 1 line (the comment)
    - grep "EXCLUDE_LIST_LOCAL" returns >= 4 lines (declaration + echo + if guard + else message)
    - grep "PHASE 8" returns 1 line in the Final Summary section
    - TOTAL_STAGES=24 still present (grep -c returns 1)
    - Stage 21b block is positioned after Stage 21 fi and before Stage 22 comment
    - The conditional uses the same scp + sudo mv + chmod 644 + chown root:root pattern as Stage 21
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configs/ru-exclude.txt (local Mac) → RPi /etc/ru-exclude.txt | Local operator file SCPed to RPi as root-owned 644 config |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | SCP of ru-exclude.txt | accept | Same trust level as all other SCP operations in deploy.sh; file is operator-authored on the same trusted machine running deploy.sh; target is root-owned 644 |
| T-08-04 | Spoofing | EXCLUDE_LIST_TMP staging path /tmp/ru-exclude.tmp | accept | Same /tmp staging pattern used for all other config files (routing.sh, dnsmasq.conf, etc.); RPi /tmp is single-user (root for these operations) |
</threat_model>

<verification>
Run after execution:

```bash
# Syntax check
bash -n deploy.sh

# All three path vars present
grep "EXCLUDE_LIST_LOCAL\|EXCLUDE_LIST_REMOTE\|EXCLUDE_LIST_TMP" deploy.sh   # expect: 3 declaration lines + usage lines

# Stage 21b block present
grep "Stage 21b" deploy.sh   # expect: 1 line

# PHASE 8 in final summary
grep "PHASE 8" deploy.sh   # expect: 1 line

# TOTAL_STAGES unchanged
grep "TOTAL_STAGES=24" deploy.sh   # expect: 1 line

# Old stage count not changed to 25
grep "TOTAL_STAGES=25" deploy.sh   # expect: no output
```
</verification>

<success_criteria>
- deploy.sh has no syntax errors (bash -n passes)
- EXCLUDE_LIST_LOCAL, EXCLUDE_LIST_REMOTE, EXCLUDE_LIST_TMP declared in path variable block
- Stage 21b conditional SCP block present after Stage 21 fi, before Stage 22
- Stage 21b echoes [21/${TOTAL_STAGES}] (same stage number, no new stage)
- Conditional follows exact white-list-extended.txt pattern: if -f guard, scp, sudo mv + chmod 644 + chown root:root, else skip message
- PHASE 8 echo line present in Final Summary after PHASE 7 line
- TOTAL_STAGES remains 24
</success_criteria>

<output>
Create `.planning/phases/08-ru-ip-list-exclusion-filter/08-02-SUMMARY.md` when done.
</output>
