---
phase: 08-ru-ip-list-exclusion-filter
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
  - 08-02
files_modified:
  - configs/ru-exclude.txt.example
  - .gitignore
  - README.md
  - docs/README.ru.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "configs/ru-exclude.txt.example exists and documents the CIDR-per-line format with # comment rules"
    - "configs/ru-exclude.txt is in .gitignore so the actual exclusion list is never committed"
    - "README.md documents the RU IP List Exclusion Filter feature: purpose, file path, format, workflow"
    - "README.md What This Does section mentions /etc/ru-exclude.txt"
    - "README.md Development Phases table has a Phase 8 row"
    - "docs/README.ru.md mirrors all README.md Phase 8 additions in Russian"
  artifacts:
    - path: "configs/ru-exclude.txt.example"
      provides: "Format documentation for the exclusion file"
      contains: "exclude[cidr4]"
    - path: ".gitignore"
      provides: "Prevents configs/ru-exclude.txt from being committed"
      contains: "configs/ru-exclude.txt"
    - path: "README.md"
      provides: "English documentation of Phase 8 feature"
      contains: "RU IP List Exclusion Filter"
    - path: "docs/README.ru.md"
      provides: "Russian documentation of Phase 8 feature"
      contains: "ru-exclude.txt"
  key_links:
    - from: "configs/ru-exclude.txt.example"
      to: "configs/ru-exclude.txt (user copy)"
      via: "cp configs/ru-exclude.txt.example configs/ru-exclude.txt"
      pattern: "ru-exclude.txt.example"
---

<objective>
Deliver four config/documentation artifacts for Phase 8:
(A) configs/ru-exclude.txt.example — committed example file mirroring white-list-extended.txt.example style
(B) .gitignore — add configs/ru-exclude.txt entry
(C) README.md — add bullet in What This Does, new section, Phase 8 row in Development Phases table,
    and a PHASE 8 entry in the deploy.sh stage groups table
(D) docs/README.ru.md — mirror all three README.md changes in Russian

Purpose: Operators have a copy-pasteable template and clear documentation for the exclusion workflow.

Output: Four files created or modified.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/ROADMAP.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/phases/08-ru-ip-list-exclusion-filter/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create configs/ru-exclude.txt.example and update .gitignore</name>
  <files>configs/ru-exclude.txt.example, .gitignore</files>
  <read_first>
    - configs/white-list-extended.txt.example — exact template to mirror (header structure, HOW TO USE
      section style, VALIDATION note, EXAMPLE ENTRIES block)
    - .gitignore — current content; add the new line after `configs/white-list-extended.txt`
  </read_first>
  <action>
Create configs/ru-exclude.txt.example with the following exact content (mirroring
white-list-extended.txt.example in structure and comment style, adapted for the exclude workflow):

```
# configs/ru-exclude.txt.example
#
# PURPOSE: Operator-defined CIDR ranges to exclude from the downloaded RU IP list.
# When /etc/ru-exclude.txt is present on the RPi, update-vpn-routes appends
# &exclude[cidr4]=CIDR query parameters to RU_SUBNET_URL before the curl download,
# so those ranges are filtered server-side and never appear in /etc/white-list.txt.
# Excluded CIDRs are therefore routed through the VPN (not the ISP).
#
# FORMAT: One CIDR per line. Lines starting with # are comments and are skipped.
# Blank lines are skipped. No inline comments after a CIDR — the whole line is
# used as-is in the URL query parameter.
#
# HOW TO USE:
#   1. Run: ssh pi4 "sudo /etc/vpn-status.sh --via=isp" to identify IPs exiting
#      via ISP that should go through the VPN (e.g. Google, Cloudflare ranges
#      that iplist incorrectly marks as RU).
#   2. Resolve the destination IP to a CIDR (e.g. via whois or ipinfo.io).
#   3. Copy this file to configs/ru-exclude.txt:
#        cp configs/ru-exclude.txt.example configs/ru-exclude.txt
#      (Note: configs/ru-exclude.txt is gitignored — it will NOT be committed.)
#   4. Add your CIDRs to configs/ru-exclude.txt (one per line, whole-line # comments only).
#   5. Run: ./deploy.sh — Stage 21 will SCP the file to /etc/ru-exclude.txt on the RPi.
#      The next update-vpn-routes run (cron or manual) will fetch the filtered list.
#
# NOTE: Changing the exclusion list changes the effective download URL, which changes
# the SHA256 of the downloaded file. The next update-vpn-routes run will detect the
# hash mismatch and rebuild routes automatically — no manual intervention needed.
#
# EXAMPLE ENTRIES (commented out — remove the leading # to activate):
# 142.250.0.0/16
# 142.251.0.0/16
```

For .gitignore: insert `configs/ru-exclude.txt` on a new line directly after the existing
`configs/white-list-extended.txt` line (currently line 9). Keep the existing comment
`# Phase 5: user-defined ISP-bypass exceptions — never commit` above white-list-extended.txt.
Add a new inline comment on the same line or a companion comment above ru-exclude.txt:

```
# Phase 8: operator CIDR exclusion list — never commit
configs/ru-exclude.txt
```

Insert these two lines after `configs/white-list-extended.txt` and before the blank line
that follows it.
  </action>
  <verify>
    <automated>
      test -f /Users/antonromankov/Projects/my-projects/split-tunneling-v2/configs/ru-exclude.txt.example && echo "example file exists"
      grep -c "ru-exclude.txt" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/.gitignore
      grep "exclude\[cidr4\]" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/configs/ru-exclude.txt.example
      grep "^# FORMAT:" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/configs/ru-exclude.txt.example
    </automated>
  </verify>
  <done>
    - configs/ru-exclude.txt.example exists
    - File contains "exclude[cidr4]" in the PURPOSE section explaining the URL parameter
    - File contains a FORMAT section describing one-CIDR-per-line with # comment rules
    - File contains a HOW TO USE section with numbered steps referencing deploy.sh Stage 21
    - File contains at least two commented-out EXAMPLE ENTRIES (142.250.0.0/16, 142.251.0.0/16)
    - .gitignore contains `configs/ru-exclude.txt` as a standalone line
    - git check-ignore -v configs/ru-exclude.txt returns a match (file would be ignored)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and docs/README.ru.md with Phase 8 documentation</name>
  <files>README.md, docs/README.ru.md</files>
  <read_first>
    - README.md — read the full file. Key insertion points:
        (a) "What This Does" section (lines 9–18): after the `**Custom exceptions**` bullet,
            add a new bullet for the exclusion filter.
        (b) "Custom Exceptions" section (line 310): add a new `## RU IP List Exclusion Filter`
            section after the existing Custom Exceptions section ends (after the verify block
            around line 378).
        (c) Deploy stage groups table (lines 119–133): the "Exceptions + NM" row covers
            stages 21–22; update that row or add detail for Stage 21b.
        (d) "Development Phases" table (lines 792–799): add a Phase 8 row after Phase 7.
    - docs/README.ru.md — read the full file to find the matching sections for all four
      changes above, so Russian equivalents are inserted in the correct positions.
  </read_first>
  <action>
Make the following four changes to README.md:

CHANGE A — "What This Does" bullet list.
After the line `- **Custom exceptions** (\`/etc/white-list-extended.txt\`) can force additional CIDRs via ISP.`
add this new bullet on the next line:
```
- **RU list exclusion filter** (`/etc/ru-exclude.txt`) can exclude specific CIDRs from the downloaded RU list so they route via VPN instead of ISP.
```

CHANGE B — New section "## RU IP List Exclusion Filter".
Insert this full section after the existing `## Custom Exceptions` section ends (after the
final verify code block in that section, before `## Rollback`):

```markdown
---

## RU IP List Exclusion Filter

Use this workflow when a CIDR range is incorrectly included in the RU IP list (e.g. a Google
or Cloudflare range that iplist marks as RU) and you want it to route through the VPN.

Unlike custom exceptions (`/etc/white-list-extended.txt`) which add ISP-bypass routes on top of
the downloaded list, the exclusion filter removes CIDRs from the downloaded list at the source —
they never appear in `/etc/white-list.txt` and therefore follow the default route (VPN).

### How it works

When `/etc/ru-exclude.txt` is present on the RPi, `update-vpn-routes` appends
`&exclude[cidr4]=CIDR` query parameters to `RU_SUBNET_URL` before calling curl. The iplist
service filters those ranges server-side. The downloaded file never contains the excluded CIDRs.

If `/etc/ru-exclude.txt` is absent or empty, the download URL is unchanged — behavior is
identical to before Phase 8.

### Setup

**Step 1: Identify CIDRs to exclude**

```bash
ssh pi4 "sudo /etc/vpn-status.sh --via=isp"
```

Look for traffic that should be routed via VPN but is exiting via ISP. Resolve the destination IP
to its network block using `whois` or `ipinfo.io`.

**Step 2: Create the exclusion file**

```bash
cp configs/ru-exclude.txt.example configs/ru-exclude.txt
```

Edit `configs/ru-exclude.txt` and add your CIDRs (one per line):

```
# Exclude Google ranges incorrectly listed as RU
142.250.0.0/16
142.251.0.0/16
```

Note: `configs/ru-exclude.txt` is gitignored and will not be committed. The `.example` file
documents the format.

**Step 3: Deploy**

```bash
./deploy.sh
```

Stage 21 SCPs `configs/ru-exclude.txt` to `/etc/ru-exclude.txt` on the RPi.

**Step 4: Trigger a route rebuild**

```bash
ssh pi4 "sudo /etc/update-vpn-routes"
```

Because the exclusion changes the effective download URL, the new list will have a different SHA256
from the existing `/etc/white-list.txt`, triggering an automatic route rebuild. Verify via:

```bash
ssh pi4 "sudo journalctl -t vpn-routes -n 10 --no-pager"
# expect: "Excluding N CIDR(s) from RU subnet download" followed by rebuild log
```

**Step 5: Verify**

```bash
ssh pi4 "ip route get <excluded-cidr-ip>"
# Expected output contains: dev awg0  (routes via VPN, not ISP)
```
```

CHANGE C — Deploy stage groups table.
Find the row: `| Exceptions + NM | 21–22 | Conditionally deploy \`white-list-extended.txt\` if present; deploy NM dispatcher \`10-vpn-routes\` |`
Update it to:
`| Exceptions + NM | 21–22 | Conditionally deploy \`white-list-extended.txt\` if present; conditionally deploy \`ru-exclude.txt\` if present; deploy NM dispatcher \`10-vpn-routes\` |`

CHANGE D — Development Phases table.
After the existing Phase 7 row, add:
```
| 7 | ASN Enrichment & Traffic Attribution | Enrich vpn-status.sh and watch-routes.py with ISP/org attribution via Team Cymru | [.planning/phases/07-asn-enrichment-traffic-attribution/](.planning/phases/07-asn-enrichment-traffic-attribution/) |
```
Wait — Phase 7 row already exists. Add a new Phase 8 row after it:
```
| 8 | RU IP List Exclusion Filter | Exclude specific CIDRs from the downloaded RU list so they route via VPN | [.planning/phases/08-ru-ip-list-exclusion-filter/](.planning/phases/08-ru-ip-list-exclusion-filter/) |
```

Also update the `deploy.sh` synopsis line in the Script CLI Reference section. Find:
`Runs from your Mac. Connects to the RPi via \`SSH_HOST=pi4\` (from \`.env\`). 23 stages.`
Update to:
`Runs from your Mac. Connects to the RPi via \`SSH_HOST=pi4\` (from \`.env\`). 24 stages.`

---

Make the same four changes to docs/README.ru.md in Russian, finding the equivalent positions
by structure (the Russian file mirrors README.md section-by-section):

Russian CHANGE A — Bullet in "Что делает эта система" (or equivalent "What This Does" section):
After the line about custom exceptions (`/etc/white-list-extended.txt`), add:
```
- **Фильтр исключений из списка РФ** (`/etc/ru-exclude.txt`) позволяет исключать отдельные CIDR-диапазоны из загружаемого списка РФ — такие диапазоны будут маршрутизироваться через VPN, а не через провайдера.
```

Russian CHANGE B — New section "## Фильтр исключений из списка РФ":
Insert after the Russian equivalent of `## Custom Exceptions` section, before `## Откат`:

```markdown
---

## Фильтр исключений из списка РФ

Используйте этот сценарий, когда CIDR-диапазон ошибочно включён в список РФ (например, диапазоны Google или Cloudflare, которые iplist помечает как российские), и вы хотите, чтобы трафик шёл через VPN.

В отличие от пользовательских исключений (`/etc/white-list-extended.txt`), которые добавляют маршруты через провайдера поверх загруженного списка, фильтр исключений удаляет CIDR-диапазоны из загружаемого списка на уровне источника — они никогда не попадают в `/etc/white-list.txt` и следуют маршруту по умолчанию (VPN).

### Принцип работы

Если `/etc/ru-exclude.txt` присутствует на RPi, `update-vpn-routes` добавляет параметры `&exclude[cidr4]=CIDR` к `RU_SUBNET_URL` перед вызовом curl. Сервис iplist фильтрует эти диапазоны на своей стороне. Загруженный файл никогда не содержит исключённых CIDR-диапазонов.

Если `/etc/ru-exclude.txt` отсутствует или пуст, URL загрузки не изменяется.

### Настройка

**Шаг 1: Определите CIDR-диапазоны для исключения**

```bash
ssh pi4 "sudo /etc/vpn-status.sh --via=isp"
```

Найдите трафик, который должен идти через VPN, но уходит через провайдера. Определите сетевой блок для нужного IP через `whois` или `ipinfo.io`.

**Шаг 2: Создайте файл исключений**

```bash
cp configs/ru-exclude.txt.example configs/ru-exclude.txt
```

Отредактируйте `configs/ru-exclude.txt` и добавьте ваши CIDR-диапазоны (по одному на строку):

```
# Исключить диапазоны Google, ошибочно отмеченные как РФ
142.250.0.0/16
142.251.0.0/16
```

Файл `configs/ru-exclude.txt` добавлен в `.gitignore` и не будет закоммичен. Файл `.example` документирует формат.

**Шаг 3: Развёртывание**

```bash
./deploy.sh
```

Stage 21 скопирует `configs/ru-exclude.txt` на RPi как `/etc/ru-exclude.txt`.

**Шаг 4: Обновление маршрутов**

```bash
ssh pi4 "sudo /etc/update-vpn-routes"
```

Так как исключения изменяют эффективный URL загрузки, новый список будет иметь другой SHA256, что автоматически запустит пересборку маршрутов.

```bash
ssh pi4 "sudo journalctl -t vpn-routes -n 10 --no-pager"
# ожидаемый вывод: "Excluding N CIDR(s) from RU subnet download" и далее пересборка
```

**Шаг 5: Проверка**

```bash
ssh pi4 "ip route get <ip-из-исключённого-диапазона>"
# Ожидаемый вывод содержит: dev awg0  (маршрут через VPN)
```
```

Russian CHANGE C — Update the deploy stages table row for Exceptions + NM (same wording update as English but in Russian).

Russian CHANGE D — Add Phase 8 row to the development phases table in Russian:
```
| 8 | Фильтр исключений из списка РФ | Исключение заданных CIDR-диапазонов из загружаемого списка РФ для маршрутизации через VPN | [.planning/phases/08-ru-ip-list-exclusion-filter/](.planning/phases/08-ru-ip-list-exclusion-filter/) |
```
  </action>
  <verify>
    <automated>
      grep -c "RU IP List Exclusion Filter\|RU list exclusion filter" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/README.md
      grep -c "ru-exclude.txt" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/README.md
      grep -c "Phase 8" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/README.md
      grep -c "ru-exclude.txt" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/docs/README.ru.md
      grep -c "Phase 8\|Фильтр исключений" /Users/antonromankov/Projects/my-projects/split-tunneling-v2/docs/README.ru.md
    </automated>
  </verify>
  <done>
    - README.md: "RU IP List Exclusion Filter" appears as a section heading
    - README.md: "ru-exclude.txt" mentioned at least 5 times (bullet + new section content + deploy table + stages table + Development Phases table)
    - README.md: Phase 8 row present in Development Phases table
    - README.md: "What This Does" section has a bullet for /etc/ru-exclude.txt
    - README.md: Deploy stage groups table updated for Stage 21 to mention ru-exclude.txt
    - docs/README.ru.md: "ru-exclude.txt" mentioned at least 5 times
    - docs/README.ru.md: Russian Phase 8 section heading present (Фильтр исключений из списка РФ or similar)
    - docs/README.ru.md: Phase 8 row in Russian development phases table
    - Both files are valid Markdown (no unclosed code fences)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configs/ru-exclude.txt (local) → .gitignore | Sensitive user data stays out of git history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Information Disclosure | configs/ru-exclude.txt committed to git | mitigate | Add configs/ru-exclude.txt to .gitignore (this plan); matches Phase 5 pattern for white-list-extended.txt |
</threat_model>

<verification>
Run after execution:

```bash
# Example file exists with correct content
test -f configs/ru-exclude.txt.example && echo "ok"
grep "exclude\[cidr4\]" configs/ru-exclude.txt.example

# gitignore covers the real file
git check-ignore -v configs/ru-exclude.txt   # expect: .gitignore:<line>:configs/ru-exclude.txt

# README English changes
grep "RU IP List Exclusion Filter" README.md   # expect: >= 1 line
grep "Phase 8" README.md   # expect: >= 1 line (Development Phases table row)
grep "ru-exclude.txt" README.md   # expect: >= 5 lines

# README Russian changes
grep "ru-exclude.txt" docs/README.ru.md   # expect: >= 5 lines
grep -i "Phase 8\|исключений" docs/README.ru.md   # expect: >= 1 line
```
</verification>

<success_criteria>
- configs/ru-exclude.txt.example created with PURPOSE, FORMAT, HOW TO USE, and EXAMPLE ENTRIES sections
- .gitignore has configs/ru-exclude.txt (git check-ignore confirms file would be ignored)
- README.md What This Does bullet added for /etc/ru-exclude.txt
- README.md has new "## RU IP List Exclusion Filter" section with 5-step workflow
- README.md Development Phases table has Phase 8 row
- README.md deploy stage groups table updated to mention ru-exclude.txt in Stage 21-22 row
- docs/README.ru.md mirrors all four changes in Russian
</success_criteria>

<output>
Create `.planning/phases/08-ru-ip-list-exclusion-filter/08-03-SUMMARY.md` when done.
</output>
