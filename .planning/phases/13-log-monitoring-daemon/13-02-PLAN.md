---
phase: "13-log-monitoring-daemon"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripts/routing.sh
  - src/scripts/update-vpn-routes
  - src/scripts/vpn-rollback.sh
  - src/configs/logrotate-vpn-gateway
  - src/configs/ru-list-exclude.txt.example
autonomous: true
requirements: []

must_haves:
  truths:
    - "All scripts write to /etc/splitgate/logs/install.log (not vpn-gateway.log)"
    - "All scripts reference /etc/splitgate/ru-list-exclude.txt (not ru-exclude.txt)"
    - "logrotate stanza targets /etc/splitgate/logs/install.log and includes postrotate for watch-*.log cleanup"
    - "ru-list-exclude.txt.example replaces ru-exclude.txt.example (git mv)"
    - "update-vpn-routes logs the download source domain, the actual excluded CIDRs, and the downloaded route count"
    - "routing.sh logs excluded CIDRs explicitly (not just a count)"
    - "vpn-rollback.sh Step 8 summary text references ru-list-exclude.txt (not ru-exclude.txt)"
  artifacts:
    - path: "src/scripts/routing.sh"
      provides: "LOG_FILE set to install.log; EXCLUDE_FILE set to ru-list-exclude.txt"
      contains: "install.log"
    - path: "src/scripts/update-vpn-routes"
      provides: "Logs domain, excluded CIDRs, route count"
      contains: "install.log"
    - path: "src/scripts/vpn-rollback.sh"
      provides: "log() targets install.log; summary text updated"
      contains: "install.log"
    - path: "src/configs/logrotate-vpn-gateway"
      provides: "Stanza targets install.log; postrotate cleans watch-*.log files older than 14 days"
      contains: "install.log"
    - path: "src/configs/ru-list-exclude.txt.example"
      provides: "Example file with updated internal references"
      contains: "ru-list-exclude.txt"
  key_links:
    - from: "src/scripts/routing.sh"
      to: "/etc/splitgate/logs/install.log"
      via: "LOG_FILE variable at top of file"
      pattern: "LOG_FILE.*install\\.log"
    - from: "src/scripts/update-vpn-routes"
      to: "/etc/splitgate/ru-list-exclude.txt"
      via: "EXCLUDE_FILE variable"
      pattern: "EXCLUDE_FILE.*ru-list-exclude"
---

<objective>
Apply two rename operations across five files and improve install log verbosity:

1. **D-07**: Rename `vpn-gateway.log` → `install.log` in routing.sh (L46), update-vpn-routes (L25), vpn-rollback.sh (L34), logrotate-vpn-gateway (L2).
2. **D-08**: Rename `ru-exclude.txt` → `ru-list-exclude.txt` in routing.sh (L92), update-vpn-routes (L41), and rename the example config file with updated internal content.
3. **D-09**: Improve install log verbosity: update-vpn-routes logs download source domain, actual excluded CIDRs (not just count), and downloaded route count; routing.sh logs excluded CIDRs explicitly.
4. **D-06**: Update logrotate stanza to target `install.log` and add `postrotate` block to clean `watch-*.log` files older than 14 days.

Note: deploy.sh is NOT modified in this plan — it is modified in Plan 03 (Wave 2) to avoid a file conflict. The `ru-exclude.txt.example` file is renamed to `ru-list-exclude.txt.example` via `git mv`.

Purpose: Consistent naming across all artifacts; clearer install logs for operational diagnosis.

Output: Updated routing.sh, update-vpn-routes, vpn-rollback.sh, logrotate-vpn-gateway; renamed ru-list-exclude.txt.example.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename log and exclude file references in routing.sh and update-vpn-routes (D-07, D-08, D-09)</name>
  <files>src/scripts/routing.sh, src/scripts/update-vpn-routes</files>

  <read_first>
    src/scripts/routing.sh — read before editing; LOG_FILE is at L46; EXCLUDE_FILE is at L92; the Stage 1 EXCLUDE_FILE loop at L93-103 logs "Stage 1: Applying ${exclude_count} exclusion(s)"
    src/scripts/update-vpn-routes — read before editing; log() hardcodes path at L25; EXCLUDE_FILE is at L41; the exclude loop at L42-53 logs "Excluding ${exclude_count} CIDR(s)"
  </read_first>

  <action>
    **In src/scripts/routing.sh:**

    1. L46: Change `LOG_FILE="/etc/splitgate/logs/vpn-gateway.log"` to `LOG_FILE="/etc/splitgate/logs/install.log"` (per D-07).

    2. L92: Change `EXCLUDE_FILE="/etc/splitgate/ru-exclude.txt"` to `EXCLUDE_FILE="/etc/splitgate/ru-list-exclude.txt"` (per D-08).

    3. Also update any comments in routing.sh that reference `ru-exclude.txt` to say `ru-list-exclude.txt` (lines 89, 157, and the comment text "ru-exclude.txt" in Stage 1 block).

    4. D-09 — Log excluded CIDRs explicitly in the EXCLUDE_FILE loop (Stage 1 block, around L93-103): instead of only logging the count, collect CIDRs into a variable during the loop and log them after. Change the current single log call `"Stage 1: Applying ${exclude_count} exclusion(s) from ${EXCLUDE_FILE}"` to two calls: one that logs the count AND lists the CIDRs, formatted as: `"Stage 1: Applying ${exclude_count} exclusion(s) from ${EXCLUDE_FILE}: ${exclude_list}"`. The `exclude_list` variable is a space-separated string built during the loop (append each CIDR with a space using `exclude_list+="${line} "`). Declare `exclude_list=""` before the loop. The log call should only fire when `exclude_count > 0` (matching existing behavior).

    **In src/scripts/update-vpn-routes:**

    1. L25: Change the hardcoded path in `log()` from `/etc/splitgate/logs/vpn-gateway.log` to `/etc/splitgate/logs/install.log` (per D-07). The full line: `log() { echo "[$(date '+%F %T')] [vpn-routes] $*" >> /etc/splitgate/logs/install.log; }`.

    2. L41: Change `EXCLUDE_FILE="/etc/splitgate/ru-exclude.txt"` to `EXCLUDE_FILE="/etc/splitgate/ru-list-exclude.txt"` (per D-08).

    3. Also update the comment at L38 that says `# If /etc/splitgate/ru-exclude.txt exists` to say `ru-list-exclude.txt`.

    4. D-09 — Log download source domain: On the line immediately after the `log "Checking for RU subnet list update..."` call (around L56), add: `log "Source: ${RU_SUBNET_URL%%/*//*/}"` — but this shell substitution is fragile; instead use the simpler approach: hardcode `log "Source: russia.iplist.opencck.org"` since the domain is fixed and never changes. Place this log call immediately before the `if ! curl ...` line.

    5. D-09 — Log actual excluded CIDRs: In the exclude loop (L42-53), mirror the routing.sh approach: declare `exclude_list=""` before the loop; inside the loop, add `exclude_list+="${line} "` after the `(( exclude_count++ ))` line. Change the existing log call `"Excluding ${exclude_count} CIDR(s) from RU subnet download"` to `"Excluding ${exclude_count} CIDR(s) from RU subnet download: ${exclude_list%% }"` (trim trailing space). This log call remains guarded by `(( exclude_count > 0 ))` as before.

    6. D-09 — Log route count after download: In Stage 3 (around L87-97), after the successful `mv "${SUBNET_TMP}" "${SUBNET_FILE}"` call, add: `route_count=$(grep -c . "${SUBNET_FILE}" || echo 0); log "Downloaded ${route_count} routes"`. Place this immediately after the mv line, before the routing.sh `--no-update` call. Note: this line is in the "hash changed → rebuild" branch only; no log is needed for the "unchanged" branch (the existing "no rebuild needed" log is sufficient there).
  </action>

  <verify>
    <automated>grep -c 'install\.log' src/scripts/routing.sh</automated>
    Expected: at least 2 (LOG_FILE definition + at least one comment reference updated)
    <automated>grep 'vpn-gateway\.log' src/scripts/routing.sh src/scripts/update-vpn-routes</automated>
    Expected: no output (zero matches)
    <automated>grep 'ru-exclude\.txt' src/scripts/routing.sh src/scripts/update-vpn-routes</automated>
    Expected: no output (zero matches — only ru-list-exclude.txt references remain)
    <automated>grep -q 'exclude_list' src/scripts/update-vpn-routes && echo OK</automated>
    Expected: OK
    <automated>grep -q 'russia\.iplist\.opencck\.org' src/scripts/update-vpn-routes && echo OK</automated>
    Expected: OK
    <automated>grep -q 'Downloaded.*routes' src/scripts/update-vpn-routes && echo OK</automated>
    Expected: OK
    <automated>bash -n src/scripts/routing.sh && echo syntax_ok</automated>
    Expected: syntax_ok
    <automated>bash -n src/scripts/update-vpn-routes && echo syntax_ok</automated>
    Expected: syntax_ok
  </verify>

  <acceptance_criteria>
    - routing.sh: LOG_FILE="/etc/splitgate/logs/install.log" (not vpn-gateway.log)
    - routing.sh: EXCLUDE_FILE="/etc/splitgate/ru-list-exclude.txt" (not ru-exclude.txt)
    - routing.sh: Stage 1 exclude log shows CIDR list, not just count
    - routing.sh: zero remaining references to "vpn-gateway.log" or "ru-exclude.txt" (in code or comments)
    - update-vpn-routes: log() writes to /etc/splitgate/logs/install.log
    - update-vpn-routes: EXCLUDE_FILE="/etc/splitgate/ru-list-exclude.txt"
    - update-vpn-routes: logs "Source: russia.iplist.opencck.org" before curl
    - update-vpn-routes: exclude log shows CIDR list, not just count
    - update-vpn-routes: logs "Downloaded N routes" after successful mv
    - update-vpn-routes: zero remaining references to "vpn-gateway.log" or "ru-exclude.txt"
    - Both files pass `bash -n` syntax check
  </acceptance_criteria>

  <done>routing.sh and update-vpn-routes use install.log and ru-list-exclude.txt throughout; install log verbosity improved per D-09.</done>
</task>

<task type="auto">
  <name>Task 2: Update vpn-rollback.sh, logrotate config, and rename example file (D-07, D-08, D-06)</name>
  <files>src/scripts/vpn-rollback.sh, src/configs/logrotate-vpn-gateway, src/configs/ru-list-exclude.txt.example</files>

  <read_first>
    src/scripts/vpn-rollback.sh — read before editing; log() is at L34; summary echo at L155 references ru-exclude.txt
    src/configs/logrotate-vpn-gateway — read before editing; only 10 lines; log path is at L2; no postrotate block currently
    src/configs/ru-exclude.txt.example — read before editing; contains references to /etc/ru-exclude.txt and configs/ru-exclude.txt at lines 4, 18-22; must be renamed and updated
  </read_first>

  <action>
    **In src/scripts/vpn-rollback.sh:**

    1. L34: Change the hardcoded path in `log()` from `/etc/splitgate/logs/vpn-gateway.log` to `/etc/splitgate/logs/install.log` (per D-07). Full line: `log() { echo "[$(date '+%F %T')] [vpn-rollback] $*" | tee -a /etc/splitgate/logs/install.log; }`.

    2. D-08 — L155: Update the Step 8 summary echo that lists removed files. The line currently says `ru-exclude.txt` — change it to `ru-list-exclude.txt`. The current text is: `echo "   /etc/splitgate/ (entire tree, including white-list.txt, isp-routes-custom.txt, vpn-routes-custom.txt, ru-exclude.txt, vpn-gateway.env, logs/)"`. Change `ru-exclude.txt` to `ru-list-exclude.txt` in that echo.

    3. Also update the file-level comment at L14 if it references `vpn-gateway.log` — change to `install.log`. Check lines 1-35 for any "vpn-gateway.log" or "ru-exclude.txt" references in comments and update them.

    **In src/configs/logrotate-vpn-gateway:**

    Rewrite the entire file content (it is only 10 lines). New content (per D-07 rename and D-06 watch-*.log cleanup):

    ```
    # src/configs/logrotate-vpn-gateway — deployed to /etc/logrotate.d/vpn-gateway (D-11, D-19). Phase 10.
    /etc/splitgate/logs/install.log {
        daily
        rotate 14
        compress
        dateext
        missingok
        notifempty
        create 0640 root root
        sharedscripts
        postrotate
            find /etc/splitgate/logs -name "watch-*.log" -mtime +14 -delete 2>/dev/null || true
        endscript
    }
    ```

    **Rename ru-exclude.txt.example → ru-list-exclude.txt.example:**

    Run: `git mv src/configs/ru-exclude.txt.example src/configs/ru-list-exclude.txt.example`

    Then update all internal content in the newly renamed `src/configs/ru-list-exclude.txt.example`. The file has references to `/etc/ru-exclude.txt` (old path, pre-Phase 10) and `configs/ru-exclude.txt` (old deploy path). Replace ALL occurrences of `ru-exclude.txt` with `ru-list-exclude.txt` inside the file. Also update any reference to `/etc/splitgate/ru-exclude.txt` to `/etc/splitgate/ru-list-exclude.txt`. Verify no `ru-exclude.txt` string remains in the file after editing.
  </action>

  <verify>
    <automated>grep 'vpn-gateway\.log' src/scripts/vpn-rollback.sh src/configs/logrotate-vpn-gateway</automated>
    Expected: no output (zero matches)
    <automated>grep 'ru-exclude\.txt' src/scripts/vpn-rollback.sh src/configs/ru-list-exclude.txt.example</automated>
    Expected: no output (zero matches)
    <automated>grep -q 'install\.log' src/configs/logrotate-vpn-gateway && echo OK</automated>
    Expected: OK
    <automated>grep -q 'watch-\*\.log' src/configs/logrotate-vpn-gateway && echo OK</automated>
    Expected: OK
    <automated>grep -q 'postrotate' src/configs/logrotate-vpn-gateway && echo OK</automated>
    Expected: OK
    <automated>test -f src/configs/ru-list-exclude.txt.example && echo RENAMED && test ! -f src/configs/ru-exclude.txt.example && echo OLD_GONE</automated>
    Expected: RENAMED OLD_GONE
    <automated>bash -n src/scripts/vpn-rollback.sh && echo syntax_ok</automated>
    Expected: syntax_ok
  </verify>

  <acceptance_criteria>
    - vpn-rollback.sh: log() writes to /etc/splitgate/logs/install.log
    - vpn-rollback.sh: Step 8 summary echo lists ru-list-exclude.txt (not ru-exclude.txt)
    - vpn-rollback.sh: zero references to "vpn-gateway.log" or "ru-exclude.txt" anywhere in file
    - vpn-rollback.sh passes bash -n syntax check
    - logrotate-vpn-gateway: targets /etc/splitgate/logs/install.log (not vpn-gateway.log)
    - logrotate-vpn-gateway: contains sharedscripts + postrotate block with `find /etc/splitgate/logs -name "watch-*.log" -mtime +14 -delete 2>/dev/null || true`
    - logrotate-vpn-gateway: contains endscript closing the postrotate block
    - src/configs/ru-exclude.txt.example no longer exists in git (renamed)
    - src/configs/ru-list-exclude.txt.example exists with zero "ru-exclude.txt" references inside
  </acceptance_criteria>

  <done>vpn-rollback.sh, logrotate-vpn-gateway, and ru-list-exclude.txt.example all use the new filenames; logrotate has postrotate watch-*.log cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logrotate postrotate → filesystem | postrotate runs `find -delete`; glob is tightly scoped to `watch-*.log` pattern — no wildcard expansion outside /etc/splitgate/logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-02-01 | Tampering | logrotate postrotate find -delete | accept | Pattern `watch-*.log` in a known owned directory; runs as root which already owns the directory; no symlink attack surface since /etc/splitgate is root-owned |
| T-13-02-02 | Information Disclosure | install.log (was vpn-gateway.log) | accept | Log file stays at 0640 root:root — same permissions as before; no new disclosure surface |
</threat_model>

<verification>
- grep 'vpn-gateway\.log' src/scripts/routing.sh src/scripts/update-vpn-routes src/scripts/vpn-rollback.sh src/configs/logrotate-vpn-gateway → no output
- grep 'ru-exclude\.txt' src/scripts/routing.sh src/scripts/update-vpn-routes src/scripts/vpn-rollback.sh src/configs/ru-list-exclude.txt.example → no output
- bash -n src/scripts/routing.sh && bash -n src/scripts/update-vpn-routes && bash -n src/scripts/vpn-rollback.sh → all exit 0
- grep -q 'postrotate' src/configs/logrotate-vpn-gateway → exits 0
- test -f src/configs/ru-list-exclude.txt.example && test ! -f src/configs/ru-exclude.txt.example → exits 0
- grep -q 'exclude_list' src/scripts/update-vpn-routes → exits 0 (D-09 verbosity)
- grep -q 'Downloaded.*routes' src/scripts/update-vpn-routes → exits 0 (D-09 route count)
</verification>

<success_criteria>
1. routing.sh: LOG_FILE and EXCLUDE_FILE use new names; Stage 1 logs CIDR list
2. update-vpn-routes: log() target, EXCLUDE_FILE, and log verbosity (domain, CIDRs, route count) updated
3. vpn-rollback.sh: log() target and Step 8 summary text updated
4. logrotate-vpn-gateway: targets install.log + postrotate watch-*.log cleanup
5. ru-list-exclude.txt.example: renamed from ru-exclude.txt.example; zero stale references inside
6. Zero occurrences of "vpn-gateway.log" or "ru-exclude.txt" in any of the five modified files
</success_criteria>

<output>
Create `.planning/phases/13-log-monitoring-daemon/13-02-SUMMARY.md` when done
</output>
