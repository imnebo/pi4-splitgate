---
phase: "13-log-monitoring-daemon"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/configs/isp-routes-custom.txt
  - src/configs/vpn-routes-custom.txt
autonomous: true
requirements: []

must_haves:
  truths:
    - "isp-routes-custom.txt contains 11 active CIDRs for confirmed-RU ranges currently routed incorrectly via VPN"
    - "vpn-routes-custom.txt contains a commented candidate block for non-RU false-positives the user can uncomment"
    - "All 11 D-01 CIDRs are uncommented/active in isp-routes-custom.txt"
    - "All D-02 candidates appear commented (prefixed with #) in vpn-routes-custom.txt"
    - "Existing 18 active CIDRs in vpn-routes-custom.txt are preserved unchanged"
  artifacts:
    - path: "src/configs/isp-routes-custom.txt"
      provides: "11 active RU CIDRs for ISP routing"
      contains: "87.228.71.0/24"
    - path: "src/configs/isp-routes-custom.txt"
      contains: "185.162.92.0/24"
    - path: "src/configs/vpn-routes-custom.txt"
      provides: "Commented non-RU VPN candidate block"
      contains: "84.32.100.0/22"
    - path: "src/configs/vpn-routes-custom.txt"
      contains: "185.199.108.0/22"
---

<objective>
Add 11 confirmed-RU CIDRs to `src/configs/isp-routes-custom.txt` so they route via ISP instead of VPN. Append a commented candidate block to `src/configs/vpn-routes-custom.txt` with non-RU false-positives the user can uncomment if a service breaks under ISP routing.

Purpose: Correct routing for Selectel, Keenetic captive portals, MIRAN-AS, RU-JSCIOT, SonicDuo, SOVAM, MegaFon, Raiffeisenbank, VimpelCom, and cloud.example.com/RU subnets that appeared as VPN-routed false-positives in the 2026-05-28 traffic log.

Output: Updated isp-routes-custom.txt and vpn-routes-custom.txt config files.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 11 RU CIDRs to isp-routes-custom.txt (D-01)</name>
  <files>src/configs/isp-routes-custom.txt</files>

  <read_first>
    src/configs/isp-routes-custom.txt — read the full file before editing; replace current content entirely, preserving the header comment block
  </read_first>

  <action>
    Rewrite src/configs/isp-routes-custom.txt. Keep the existing header comment block at the top (lines 1-17, starting with "# configs/isp-routes-custom.txt"), then append the following 11 active CIDRs in order, each preceded by a single-line comment naming the AS/org (per D-01). No inline comments on the CIDR lines themselves — comments on their own lines only.

    Active CIDRs to add (per D-01), in this exact order with named comments:
    - comment: "# Selectel (JSC Selectel, RU) — seen via VPN" then CIDR: 87.228.71.0/24
    - comment: "# Selectel (JSC Selectel, RU) — seen via VPN" then CIDR: 95.213.181.0/24
    - comment: "# MIRAN-AS (captive116.keenetic.ru) — seen via VPN" then CIDR: 185.162.92.0/24
    - comment: "# RU-JSCIOT (captive113/115.keenetic.ru) — seen via VPN" then CIDR: 89.169.29.0/24
    - comment: "# RU datacenter (vlan1354.dci6) — seen via VPN" then CIDR: 31.135.14.0/24
    - comment: "# SonicDuo-AS (RU) — seen via VPN" then CIDR: 31.173.34.0/24
    - comment: "# SOVAM-AS (RU) — seen via VPN" then CIDR: 81.9.21.0/24
    - comment: "# MegaFon PJSC (RU) — seen via VPN" then CIDR: 85.26.231.0/24
    - comment: "# Raiffeisenbank RBRU-AS (RU) — seen via VPN" then CIDR: 193.28.44.0/24
    - comment: "# VimpelCom/Corbina (RU) — seen via VPN" then CIDR: 128.75.237.0/24
    - comment: "# RU ASN (cloud.example.com, ASN confirmed RU) — seen via VPN" then CIDR: 178.249.69.0/24

    Update the "Log analysis" comment block in the header to reflect that 11 RU CIDRs were added from the 2026-05-28 analysis and now appear as active entries below. Replace the phrase "No non-Russian candidates found that require explicit ISP routing" with a note referencing the 11 active CIDRs below.
  </action>

  <verify>
    <automated>grep -c '^[0-9]' src/configs/isp-routes-custom.txt</automated>
    Expected output: 11
    Also verify: grep -q '87.228.71.0/24' src/configs/isp-routes-custom.txt && grep -q '185.162.92.0/24' src/configs/isp-routes-custom.txt && grep -q '178.249.69.0/24' src/configs/isp-routes-custom.txt && echo OK
    Expected: OK
  </verify>

  <acceptance_criteria>
    - File contains exactly 11 uncommented CIDR lines (lines starting with a digit)
    - All 11 D-01 CIDRs present: 87.228.71.0/24, 95.213.181.0/24, 185.162.92.0/24, 89.169.29.0/24, 31.135.14.0/24, 31.173.34.0/24, 81.9.21.0/24, 85.26.231.0/24, 193.28.44.0/24, 128.75.237.0/24, 178.249.69.0/24
    - No inline comments on CIDR lines (all comment text is on separate # lines)
    - Header comment block preserved and updated to reference the 11 active entries
    - File ends with a newline
  </acceptance_criteria>

  <done>isp-routes-custom.txt contains 11 active RU CIDRs from D-01, each with an identifying comment on the preceding line.</done>
</task>

<task type="auto">
  <name>Task 2: Append commented non-RU candidate block to vpn-routes-custom.txt (D-02)</name>
  <files>src/configs/vpn-routes-custom.txt</files>

  <read_first>
    src/configs/vpn-routes-custom.txt — read the full file before editing; the existing 18 active CIDRs must be preserved unchanged; only append a new section at the bottom
  </read_first>

  <action>
    Append a new section to the end of src/configs/vpn-routes-custom.txt (after the existing 18 active CIDRs). Do not modify any existing content.

    The appended section must start with a blank line, then a header comment block:
    "# ─── Non-RU VPN candidates (commented — uncomment if a service fails via ISP) ───"
    "# Source: log analysis 2026-05-28. These are non-RU ranges that were routing via ISP"
    "# as false-positives. Uncomment to force via VPN."

    Then add these commented candidate entries in order (comment with org/source on the line before each commented CIDR):
    - comment: "# Cherry Servers LT — 325 hits via ISP (largest non-RU false-positive)" then: #84.32.100.0/22
    - blank line
    - comment: "# Google PoPs (lhr/par/tzhema/ncberi) — seen via ISP" then: #192.178.25.0/24, #192.178.170.0/24, #216.58.198.0/24, #216.58.201.0/24, #216.58.207.0/24 (one per line, each with # prefix)
    - blank line
    - comment: "# Cloudflare non-DNS — seen via ISP" then: #8.6.112.0/24, #8.47.69.0/24 (one per line, each with # prefix)
    - blank line
    - comment: "# Amazon CloudFront hel51 — seen via ISP" then: #18.165.122.0/24
    - comment: "# Amazon EC2 ap-southeast-1 — seen via ISP" then: #13.228.133.0/24
    - blank line
    - comment: "# Akamai NL/US — seen via ISP" then: #2.16.238.0/24, #2.17.251.0/24, #2.22.145.0/24, #2.23.88.0/24, #23.11.40.0/24, #23.216.134.0/24, #95.100.107.0/24, #95.101.27.0/24, #184.86.251.0/24 (one per line, each with # prefix)
    - blank line
    - comment: "# Microsoft Azure EU — seen via ISP" then: #51.116.246.0/24, #51.116.253.0/24 (one per line, each with # prefix)

    All candidate CIDRs must be prefixed with # (commented out). Each CIDR is on its own line. No inline comments on CIDR lines.
  </action>

  <verify>
    <automated>grep -v '^#' src/configs/vpn-routes-custom.txt | grep -c '^[0-9]'</automated>
    Expected output: 18 (existing active CIDRs — unchanged)
    Also: grep -c '^#84\.32\.100\.0/22' src/configs/vpn-routes-custom.txt
    Expected output: 1
    Also: grep -c '^#51\.116\.246\.0/24' src/configs/vpn-routes-custom.txt
    Expected output: 1
  </verify>

  <acceptance_criteria>
    - Existing 18 active CIDRs unchanged (all lines that previously started with a digit still present and identical)
    - Candidate block appended at the bottom after existing content
    - All D-02 candidates present as commented lines: 84.32.100.0/22, 192.178.25.0/24, 192.178.170.0/24, 216.58.198.0/24, 216.58.201.0/24, 216.58.207.0/24, 8.6.112.0/24, 8.47.69.0/24, 18.165.122.0/24, 13.228.133.0/24, 2.16.238.0/24, 2.17.251.0/24, 2.22.145.0/24, 2.23.88.0/24, 23.11.40.0/24, 23.216.134.0/24, 95.100.107.0/24, 95.101.27.0/24, 184.86.251.0/24, 51.116.246.0/24, 51.116.253.0/24
    - Every candidate CIDR has # prefix (commented — not active)
    - Header section comment present at start of new block
    - File ends with a newline
  </acceptance_criteria>

  <done>vpn-routes-custom.txt has its 18 existing active entries unchanged and a new commented candidate section appended with all D-02 CIDRs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text file → routing.sh | CIDRs in these files are passed directly as arguments to `ip route add` — no eval; malformed lines suppressed by `2>/dev/null || true` (T-02-02 pattern) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | isp-routes-custom.txt / vpn-routes-custom.txt | accept | Files committed to git; routing.sh passes CIDRs as direct args to ip route add (no eval); malformed entries silently skipped with `|| true` |
</threat_model>

<verification>
- grep -c '^[0-9]' src/configs/isp-routes-custom.txt → 11
- grep -v '^#' src/configs/vpn-routes-custom.txt | grep -c '^[0-9]' → 18
- grep -q '87.228.71.0/24' src/configs/isp-routes-custom.txt → exits 0
- grep -q '^#84\.32\.100\.0/22' src/configs/vpn-routes-custom.txt → exits 0
- python3 -c "lines=[l.strip() for l in open('src/configs/isp-routes-custom.txt') if l.strip() and not l.startswith('#')]; assert len(lines)==11, lines"
</verification>

<success_criteria>
1. isp-routes-custom.txt: exactly 11 active (uncommented) CIDR lines — all from D-01
2. vpn-routes-custom.txt: 18 active CIDRs unchanged + new commented candidate block with all D-02 CIDRs
3. No inline comments on any CIDR line in either file
</success_criteria>

<output>
Create `.planning/phases/13-log-monitoring-daemon/13-01-SUMMARY.md` when done
</output>
