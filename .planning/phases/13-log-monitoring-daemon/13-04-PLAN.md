---
phase: "13-log-monitoring-daemon"
plan: "04"
type: execute
wave: 3
depends_on:
  - "13-01"
  - "13-02"
  - "13-03"
files_modified:
  - README.md
  - docs/README.ru.md
  - docs/REFERENCE.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "README.md reflects: splitgate-watch.service daemon, install.log, ru-list-exclude.txt names"
    - "docs/README.ru.md is a faithful Russian translation of README.md including Phase 13 changes"
    - "docs/REFERENCE.md has updated log file names, updated exclude file name, new --daemon flag usage, new splitgate-watch.service section"
    - "STATE.md records all Phase 13 decisions (D-07, D-08, D-09, D-10 and the routing config additions D-01, D-02)"
    - "ROADMAP.md Phase 13 entry has plan list and status updated"
    - "Zero stale references to vpn-gateway.log or ru-exclude.txt remain in README.md, docs/README.ru.md, docs/REFERENCE.md"
  artifacts:
    - path: "README.md"
      provides: "Updated references for Phase 13 changes"
      contains: "install.log"
    - path: "docs/README.ru.md"
      provides: "Russian translation updated for Phase 13"
      contains: "install.log"
    - path: "docs/REFERENCE.md"
      provides: "Technical reference with --daemon, splitgate-watch.service, updated log/exclude names"
      contains: "splitgate-watch"
    - path: ".planning/STATE.md"
      provides: "Decisions D-07 through D-10 and D-01/D-02 (routing config) recorded"
      contains: "D-07"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 entry with 4 plans listed"
      contains: "13-01-PLAN.md"
---

<objective>
Update all documentation artifacts to reflect Phase 13 changes: renamed log file (install.log), renamed exclude file (ru-list-exclude.txt), new --daemon flag and watch-routes.py daemon mode, new splitgate-watch.service, new RU CIDRs in isp-routes-custom.txt, and non-RU candidate block in vpn-routes-custom.txt.

Purpose: Satisfy CLAUDE.md requirement — after completing any task, review README.md, docs/README.ru.md, and docs/REFERENCE.md; update if touched areas are covered. Phase 13 touches log names, config file names, a new systemd service, and the daemon workflow — all three docs must be updated. Before closing, also run the full stale-reference audit for all renamed concepts.

Output: Updated README.md, docs/README.ru.md, docs/REFERENCE.md, .planning/STATE.md, .planning/ROADMAP.md.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-log-monitoring-daemon/13-01-SUMMARY.md
@.planning/phases/13-log-monitoring-daemon/13-02-SUMMARY.md
@.planning/phases/13-log-monitoring-daemon/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full stale-reference audit + update README.md, docs/README.ru.md, docs/REFERENCE.md</name>
  <files>README.md, docs/README.ru.md, docs/REFERENCE.md</files>

  <read_first>
    README.md — read full file before editing
    docs/README.ru.md — read full file before editing
    docs/REFERENCE.md — read full file before editing
  </read_first>

  <action>
    **Step 0 — Stale reference audit (CLAUDE.md requirement):**

    Before editing any doc, run these grep commands to find all stale references in tracked files:

    1. `grep -rn 'vpn-gateway\.log' --include='*.md' --include='*.sh' --include='*.py' --include='*.txt' --include='*.service' --include='*.conf' .` — must return zero results after Plan 02 and 03. If results found in docs, those are the lines to fix in this task.

    2. `grep -rn 'ru-exclude\.txt' --include='*.md' --include='*.sh' --include='*.py' --include='*.txt' --include='*.service' --include='*.conf' .` — must return zero results after Plan 02 and 03. Fix any in docs.

    3. `grep -rn 'splitgate-watch' --include='*.md' . | grep -v 'REFERENCE\|README'` — should return no unexpected files.

    **For README.md:**

    Read the current README.md. It is a trimmed 3-section format (from Phase 11). Locate any references to:
    - `vpn-gateway.log` → change to `install.log`
    - `ru-exclude.txt` → change to `ru-list-exclude.txt`

    Add or update the commands section (the section covering `splitgate watch` or route-watching) to mention:
    - The daemon mode: `splitgate watch --daemon` or `python3 /etc/splitgate/watch-routes.py --daemon` runs as a persistent service
    - Log files are at `/etc/splitgate/logs/watch-YYYY-MM-DD.log`
    - The service name is `splitgate-watch.service` (managed by `systemctl status splitgate-watch`)
    - Grep pattern for checking ISP route failures: `grep "[ISP] ✗" /etc/splitgate/logs/watch-$(date +%F).log`

    If README.md does not currently have a route-watching section, add a brief one under an appropriate existing section heading without restructuring the document.

    **For docs/README.ru.md:**

    Apply identical changes to the Russian translation: all references to `vpn-gateway.log` → `install.log`, all `ru-exclude.txt` → `ru-list-exclude.txt`. Translate the daemon mode additions from README.md into Russian — maintain the same section structure as the English version. Keep terminology consistent with existing Russian text (e.g., use the same terms for "маршрутизация", "лог-файл" etc. that already appear in the file).

    **For docs/REFERENCE.md:**

    Read the full file and locate relevant sections. Make the following targeted updates:

    1. Log file references: Replace `vpn-gateway.log` with `install.log` everywhere. Update examples that show log paths.

    2. Exclude file references: Replace `ru-exclude.txt` with `ru-list-exclude.txt` everywhere. Update the section that describes creating the exclusion file and references `configs/ru-exclude.txt`.

    3. Add or update the `watch-routes.py` / `splitgate watch` section to document:
       - `--daemon` flag: "Writes to /etc/splitgate/logs/watch-YYYY-MM-DD.log instead of stdout. Used by splitgate-watch.service."
       - Output format in daemon mode: `2026-05-29T10:14:00 [ISP] ✓ 10.0.0.237 → yandex.ru TCP:443 | TELETECH, RU`
       - Status field: `✓` = connection ESTABLISHED in conntrack; `✗` = not found (UDP always ✗)
       - Log rotation: new dated file opened at midnight; files older than 14 days deleted by logrotate
       - Useful grep: `grep "[ISP] ✗" /etc/splitgate/logs/watch-$(date +%F).log`
       - `--no-asn` + `--daemon`: line written without status field

    4. Add a `splitgate-watch.service` section (or sub-section) covering:
       - What it does: runs `watch-routes.py --daemon` at boot, restarts on failure
       - Start/stop: `systemctl start/stop splitgate-watch` (or `splitgate watch` when using dispatcher)
       - Status: `systemctl status splitgate-watch`
       - Logs: /etc/splitgate/logs/watch-YYYY-MM-DD.log, errors: /etc/splitgate/logs/watch-error.log

    5. Routing config files section: Note that isp-routes-custom.txt now has 11 active RU CIDRs (Selectel, MIRAN-AS/Keenetic captive portals, etc.) and vpn-routes-custom.txt has a commented candidate block for non-RU false-positives.

    Do not restructure the document; insert updates in the relevant existing sections or append new sub-sections where appropriate.
  </action>

  <verify>
    <automated>grep -rn 'vpn-gateway\.log' README.md docs/README.ru.md docs/REFERENCE.md</automated>
    Expected: no output
    <automated>grep -rn 'ru-exclude\.txt' README.md docs/README.ru.md docs/REFERENCE.md</automated>
    Expected: no output
    <automated>grep -q 'splitgate-watch' docs/REFERENCE.md && echo OK</automated>
    Expected: OK
    <automated>grep -q 'install\.log' README.md && echo OK</automated>
    Expected: OK
    <automated>grep -q 'watch-.*\.log' docs/REFERENCE.md && echo OK</automated>
    Expected: OK
    <automated>grep -q '\-\-daemon' docs/REFERENCE.md && echo OK</automated>
    Expected: OK
  </verify>

  <acceptance_criteria>
    - README.md: zero occurrences of "vpn-gateway.log" or "ru-exclude.txt"
    - README.md: references install.log; mentions splitgate-watch.service and daemon log path
    - docs/README.ru.md: zero occurrences of "vpn-gateway.log" or "ru-exclude.txt"
    - docs/README.ru.md: Russian translation consistent with README.md Phase 13 additions
    - docs/REFERENCE.md: zero occurrences of "vpn-gateway.log" or "ru-exclude.txt"
    - docs/REFERENCE.md: --daemon flag documented with output format example
    - docs/REFERENCE.md: ✓/✗ status field explained (conntrack ESTABLISHED/TIME_WAIT)
    - docs/REFERENCE.md: splitgate-watch.service section present (start/stop/status/logs)
    - docs/REFERENCE.md: grep pattern example for ISP ✗ lines
    - docs/REFERENCE.md: isp-routes-custom.txt and vpn-routes-custom.txt candidate block noted
  </acceptance_criteria>

  <done>All three documentation files updated with Phase 13 changes; zero stale vpn-gateway.log or ru-exclude.txt references in any doc.</done>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md decisions and ROADMAP.md Phase 13 entry</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>

  <read_first>
    .planning/STATE.md — read full file; locate the "## Decisions" section; decisions currently go up to D-19 / Phase 10; new Phase 13 decisions go after the most recent entry
    .planning/ROADMAP.md — read the Phase 13 section at the end of the file; update plan list and status
  </read_first>

  <action>
    **In .planning/STATE.md:**

    1. Update the header metadata:
       - `status:` → `Phase 13 complete — daemon mode + routing refinement + renames (4/4 plans)`
       - `stopped_at:` → `Phase 13 Plan 04 complete — splitgate-watch.service; install.log + ru-list-exclude.txt renames; 11 RU CIDRs added; daemon with conntrack ✓/✗`
       - `last_updated:` → `"2026-05-29T00:00:00Z"`
       - `progress.total_phases:` → 13
       - `progress.completed_phases:` → 13
       - `progress.total_plans:` → 32
       - `progress.completed_plans:` → 32
       - `progress.percent:` → 100

    2. Update `## Current Phase` section to say `Phase 13: Log Monitoring, Routing Refinement & Daemon — COMPLETE (4/4 plans)`.

    3. Update the `## Phase Progress` table — add Phase 13 row:
       `| 13 — Log Monitoring, Routing Refinement & Daemon | ✓ Complete | 4/4 done | 100% |`

    4. In `## Decisions`, append new decision entries after the last existing entry (Phase 9/10/12 entries). Add the following (adapt exact wording to match the project's decision log style):

       ```
       - Phase 13 D-01: isp-routes-custom.txt gains 11 active /24 RU CIDRs (Selectel ×2, MIRAN-AS/Keenetic captive, RU-JSCIOT/Keenetic captive, SonicDuo, SOVAM, MegaFon, Raiffeisenbank, VimpelCom/Corbina, cloud.example.com/RU ASN) — confirmed RU, previously routing via VPN
       - Phase 13 D-02: vpn-routes-custom.txt gains commented candidate block for non-RU false-positives (Cherry Servers LT, Google PoPs ×5, Cloudflare non-DNS ×2, CloudFront hel51, EC2 ap-se-1, Akamai NL/US ×9, Azure EU ×2) — user uncomments if service fails
       - Phase 13 D-03: watch-routes.py connection status via /proc/net/nf_conntrack; STATUS_DELAY=3s, DEDUP_TTL=30s; ✓=ESTABLISHED or TIME_WAIT; ✗=not found (UDP always ✗)
       - Phase 13 D-04: output format: `{ISO_TS} [{tag}] {status} {src} → {dst} {proto}:{port} | {org}` — status between [TAG] and src; always present in daemon mode
       - Phase 13 D-05: --daemon flag — without it: stdout unchanged; with it: writes to dated log, no stdout
       - Phase 13 D-06: LOG_DIR=/etc/splitgate/logs; dated log filename watch-YYYY-MM-DD.log; midnight rotation via date.today() check per write; cleanup via logrotate postrotate find -mtime +14
       - Phase 13 D-07: vpn-gateway.log renamed to install.log across routing.sh, update-vpn-routes, vpn-rollback.sh, logrotate-vpn-gateway, deploy.sh summary
       - Phase 13 D-08: ru-exclude.txt renamed to ru-list-exclude.txt across routing.sh, update-vpn-routes, deploy.sh vars+stage+summary, ru-list-exclude.txt.example; deploy.sh Stage 22c adds live RPi migration
       - Phase 13 D-09: install log verbosity — update-vpn-routes logs source domain (russia.iplist.opencck.org), actual excluded CIDRs list, downloaded route count; routing.sh logs excluded CIDRs list in Stage 1
       - Phase 13 D-10: splitgate-watch.service (ExecStart: watch-routes.py --daemon; StandardOutput=null; StandardError=append watch-error.log; Restart=on-failure RestartSec=5 User=root); deploy.sh Stage 27 + TOTAL_STAGES=28; vpn-rollback.sh stops/disables service in Step 1
       ```

    5. Update `## Last Session`:
       ```
       **Stopped at:** Phase 13 Plan 04 complete — all docs updated
       **Timestamp:** 2026-05-29T00:00:00Z
       **Resume:** All 13 phases complete. Next: deploy to RPi and verify splitgate-watch.service.
       ```

    **In .planning/ROADMAP.md:**

    Locate the Phase 13 entry (`### Phase 13: Log Monitoring, Routing Refinement & Daemon`). Update:

    1. Replace `**Plans:** TBD` with `**Plans:** 4 plans`

    2. Replace the `- [ ] TBD (run /gsd:plan-phase 13 to break down)` line with the plan list:
       ```
       Plans:

       **Wave 1 (parallel):**

       - [x] 13-01-PLAN.md — Routing config updates (isp-routes-custom.txt + vpn-routes-custom.txt) ✓ 2026-05-29
       - [x] 13-02-PLAN.md — System cleanup: install.log + ru-list-exclude.txt renames, install log verbosity, logrotate postrotate ✓ 2026-05-29

       **Wave 2 (after Wave 1):**

       - [x] 13-03-PLAN.md — watch-routes.py daemon mode (--daemon, ✓/✗ status, dedup) + splitgate-watch.service + deploy.sh Stage 27 + vpn-rollback.sh ✓ 2026-05-29

       **Wave 3 (after all):**

       - [x] 13-04-PLAN.md — Documentation (README.md, docs/README.ru.md, docs/REFERENCE.md, STATE.md) ✓ 2026-05-29
       ```

    3. Update the overview table row for Phase 13: change `◆ Planned` (or whatever current status is) to `✓ Complete`.

    4. Add `**Phase 13 complete ✓**` after the plan list.
  </action>

  <verify>
    <automated>grep -q 'Phase 13 D-07' .planning/STATE.md && echo OK</automated>
    Expected: OK
    <automated>grep -q 'Phase 13 D-10' .planning/STATE.md && echo OK</automated>
    Expected: OK
    <automated>grep -q '13-01-PLAN.md' .planning/ROADMAP.md && echo OK</automated>
    Expected: OK
    <automated>grep -q '13-04-PLAN.md' .planning/ROADMAP.md && echo OK</automated>
    Expected: OK
    <automated>grep -q 'TOTAL_STAGES=28\|total_plans: 32' .planning/STATE.md</automated>
    Expected: total_plans: 32 is present
    <automated>grep 'completed_plans' .planning/STATE.md</automated>
    Expected: completed_plans: 32
  </verify>

  <acceptance_criteria>
    - STATE.md: status line reflects Phase 13 complete
    - STATE.md: total_phases=13, completed_phases=13, total_plans=32, completed_plans=32
    - STATE.md: Phase 13 row in Phase Progress table showing ✓ Complete
    - STATE.md: all 10 Phase 13 decisions recorded (D-01 through D-10)
    - STATE.md: Last Session updated to 2026-05-29
    - ROADMAP.md: Phase 13 overview table shows ✓ Complete
    - ROADMAP.md: 4 plan entries present (13-01 through 13-04) with wave structure
    - ROADMAP.md: "Phase 13 complete ✓" marker present
  </acceptance_criteria>

  <done>STATE.md and ROADMAP.md fully reflect Phase 13 completion with all decisions recorded and plan list updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → reader | Documentation only; no execution path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-01 | Information Disclosure | docs showing log file paths | accept | Paths are non-secret; already documented in existing README/REFERENCE; no new sensitive data exposed |
</threat_model>

<verification>
- grep -rn 'vpn-gateway\.log' README.md docs/README.ru.md docs/REFERENCE.md → no output
- grep -rn 'ru-exclude\.txt' README.md docs/README.ru.md docs/REFERENCE.md → no output
- grep -q 'splitgate-watch' docs/REFERENCE.md → exits 0
- grep -q '\-\-daemon' docs/REFERENCE.md → exits 0
- grep -q 'Phase 13 D-07' .planning/STATE.md → exits 0
- grep -q '13-04-PLAN.md' .planning/ROADMAP.md → exits 0
- Full stale ref audit: grep -rn 'vpn-gateway\.log\|ru-exclude\.txt' --include='*.md' --include='*.sh' --include='*.py' --include='*.txt' --include='*.service' . → zero results
</verification>

<success_criteria>
1. README.md + docs/README.ru.md: zero stale log/exclude filenames; daemon workflow documented
2. docs/REFERENCE.md: --daemon flag, ✓/✗ status, splitgate-watch.service, log paths, grep example documented; zero stale names
3. STATE.md: 10 Phase 13 decisions logged; progress counters updated to 13/13 phases, 32/32 plans
4. ROADMAP.md: Phase 13 marked Complete with 4 plans listed by wave
5. Full project-wide stale reference audit passes (zero vpn-gateway.log or ru-exclude.txt in any tracked file)
</success_criteria>

<output>
Create `.planning/phases/13-log-monitoring-daemon/13-04-SUMMARY.md` when done
</output>
