---
phase: 10-splitgate-ergonomics
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/scripts/routing.sh
  - src/scripts/vpn-status.sh
  - src/scripts/vpn-rollback.sh
  - src/scripts/update-vpn-routes
  - src/scripts/watch-routes.py
  - src/scripts/10-vpn-routes
  - src/scripts/asn-lookup.py
  - src/scripts/install-awg.sh
  - src/configs/dnsmasq.conf
  - src/configs/amnezia.key.template.txt
  - src/systemd/vpn-routing.service
  - src/deploy.sh
  - .gitignore
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/ directory exists containing scripts/, configs/, systemd/, tests/, deploy.sh"
    - "No scripts/, configs/, systemd/, tests/ directories remain at repo root; deploy.sh absent from repo root"
    - "src/deploy.sh navigates to its own directory at startup via cd so all *_LOCAL relative paths remain correct without modification"
    - ".gitignore references src/configs/white-list-extended.txt and src/configs/ru-exclude.txt (not old configs/ bare paths)"
  artifacts:
    - path: "src/deploy.sh"
      provides: "Deploy orchestrator at new path, auto-navigating to src/ at startup"
      contains: "cd \"$(dirname \"${BASH_SOURCE[0]}\")\""
    - path: ".gitignore"
      provides: "Updated ignore rules with src/ prefix on configs/ entries"
      contains: "src/configs/white-list-extended.txt"
  key_links:
    - from: "src/deploy.sh"
      to: "src/scripts/, src/configs/, src/systemd/"
      via: "cd at startup makes all *_LOCAL relative paths resolve to src/ subdirectories"
      pattern: "cd \"$(dirname"
---

<objective>
Consolidate all source code, scripts, configs, and deployment tooling into a `src/` subdirectory of the repo. This is a pure structural reorganization — no content changes to scripts or configs, only their location within the repo tree.

After this plan runs:
- `scripts/` → `src/scripts/`
- `configs/` → `src/configs/`
- `systemd/` → `src/systemd/`
- `tests/` → `src/tests/`
- `amnezia.key.template.txt` (repo root) → `src/configs/amnezia.key.template.txt`
- `deploy.sh` (repo root) → `src/deploy.sh`

`src/deploy.sh` gains a `cd "$(dirname "${BASH_SOURCE[0]}")"` call near the top so all `*_LOCAL` relative path variables (e.g. `ROUTING_SH_LOCAL="scripts/routing.sh"`) continue to resolve correctly without modification — they are now relative to `src/`, which is the script's own directory. The `.env` / `.env.secrets` files stay at repo root and are accessed via `../.env` after the cd. `TEMPLATE` is updated to `configs/amnezia.key.template.txt` since `amnezia.key.template.txt` has moved into `src/configs/`.

`.gitignore` entries that reference `configs/white-list-extended.txt` and `configs/ru-exclude.txt` are updated to `src/configs/white-list-extended.txt` and `src/configs/ru-exclude.txt`.

Purpose: Plans 10-01 and 10-02 (Wave 1) and Plan 10-03 (Wave 2) operate on files under `src/`. This Wave 0 plan must complete and commit before those plans execute.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-splitgate-ergonomics/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move all source directories and files into src/ via git mv</name>
  <files>src/scripts/, src/configs/, src/systemd/, src/tests/, src/deploy.sh</files>
  <read_first>
    - scripts/ — verify all files present before moving (ls scripts/)
    - configs/ — verify all files present before moving (ls configs/)
    - systemd/ — verify all files present before moving (ls systemd/)
    - tests/ — verify all files present before moving (ls tests/)
    - deploy.sh — verify it exists at repo root before moving
    - amnezia.key.template.txt — verify it exists at repo root before moving
  </read_first>
  <action>
    From the repo root, execute git mv in this exact order (git mv preserves history):

    1. `mkdir -p src`
    2. `git mv scripts src/scripts`
    3. `git mv configs src/configs`
    4. `git mv systemd src/systemd`
    5. `git mv tests src/tests`
    6. `git mv amnezia.key.template.txt src/configs/amnezia.key.template.txt`
    7. `git mv deploy.sh src/deploy.sh`

    After all moves verify with `ls` that: repo root has no `scripts/`, `configs/`, `systemd/`, `tests/` directories and no `deploy.sh` or `amnezia.key.template.txt`; `src/` contains all of them.

    Do NOT use `mv` — use `git mv` so git tracks the renames and history is preserved. Do NOT move: `.env`, `.env.secrets`, `.env.example`, `.env.secrets.example`, `README.md`, `SPEC.md`, `CLAUDE.md`, `docs/`, `.gitignore`, `.planning/`, `.claude/`.
  </action>
  <verify>
    <automated>test -d src/scripts && test -d src/configs && test -d src/systemd && test -d src/tests && test -f src/deploy.sh && test -f src/configs/amnezia.key.template.txt && ! test -d scripts && ! test -d configs && ! test -d systemd && ! test -d tests && ! test -f deploy.sh && ! test -f amnezia.key.template.txt</automated>
  </verify>
  <done>
    - `test -d src/scripts` exits 0.
    - `test -d src/configs` exits 0.
    - `test -d src/systemd` exits 0.
    - `test -d src/tests` exits 0.
    - `test -f src/deploy.sh` exits 0.
    - `test -f src/configs/amnezia.key.template.txt` exits 0.
    - `test -d scripts` exits 1 (no longer at repo root).
    - `test -d configs` exits 1 (no longer at repo root).
    - `test -f deploy.sh` exits 1 (no longer at repo root).
    - `git status --short | grep -cE '^R' | tr -d ' '` is at least 6 (all renames staged).
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit src/deploy.sh — add cd navigation, update .env and TEMPLATE paths</name>
  <files>src/deploy.sh</files>
  <read_first>
    - src/deploy.sh — the entire file. Focus on: (1) the `set -euo pipefail` line (add cd immediately after); (2) `TEMPLATE="amnezia.key.template.txt"` declaration (update path); (3) the preflight checks `if [[ ! -f .env ]]` and `if [[ ! -f .env.secrets ]]` (update paths); (4) `source .env` and `source .env.secrets` lines; (5) `scp .env "${SSH_HOST}:/tmp/vpn-gateway.env.tmp"` SCP line; (6) all error messages that say "run from the repo root".
  </read_first>
  <action>
    Make the following targeted edits to `src/deploy.sh`:

    1. **Add cd navigation.** Immediately after the line `set -euo pipefail`, insert this block:
       ```
       # Navigate to script's own directory so all *_LOCAL relative paths resolve correctly.
       cd "$(dirname "${BASH_SOURCE[0]}")"
       ```
       This means deploy.sh can be invoked as `bash src/deploy.sh` from anywhere; all `*_LOCAL` vars (e.g. `ROUTING_SH_LOCAL="scripts/routing.sh"`) now resolve to `src/scripts/routing.sh` relative to the repo root.

    2. **Update TEMPLATE path.** Change:
       `TEMPLATE="amnezia.key.template.txt"` → `TEMPLATE="configs/amnezia.key.template.txt"`
       (the template has moved into src/configs/)

    3. **Update .env preflight checks.** Change:
       `if [[ ! -f .env ]]` → `if [[ ! -f ../.env ]]`
       `if [[ ! -f .env.secrets ]]` → `if [[ ! -f ../.env.secrets ]]`

    4. **Update .env source lines.** Change:
       `source .env` → `source ../.env`
       `source .env.secrets` → `source ../.env.secrets`

    5. **Update .env SCP line.** Change:
       `scp .env "${SSH_HOST}:/tmp/vpn-gateway.env.tmp"` → `scp ../.env "${SSH_HOST}:/tmp/vpn-gateway.env.tmp"`

    6. **Update error messages.** Change all occurrences of the phrase `run from the repo root` in error messages to `run as: bash src/deploy.sh` so operators know the correct invocation path after the restructuring.

    Do NOT change any `*_LOCAL` variable values (e.g. `ROUTING_SH_LOCAL="scripts/routing.sh"` stays as-is — it is now relative to src/ after the cd). Do NOT change any `*_REMOTE` variable values. Do NOT change any other logic.
  </action>
  <verify>
    <automated>bash -n src/deploy.sh && grep -q 'cd "$(dirname "${BASH_SOURCE\[0\]}")"' src/deploy.sh && grep -q 'TEMPLATE="configs/amnezia.key.template.txt"' src/deploy.sh && grep -q 'source ../\.env' src/deploy.sh && grep -q 'source ../\.env\.secrets' src/deploy.sh && ! grep -q 'source \.env$' src/deploy.sh && ! grep -q "scp \.env " src/deploy.sh</automated>
  </verify>
  <done>
    - `bash -n src/deploy.sh` exits 0.
    - `grep -c 'cd "$(dirname "${BASH_SOURCE\[0\]}")"' src/deploy.sh` is exactly 1.
    - `grep -c 'TEMPLATE="configs/amnezia.key.template.txt"' src/deploy.sh` is exactly 1.
    - `grep -c 'source ../\.env$' src/deploy.sh` is exactly 1.
    - `grep -c 'source ../\.env\.secrets$' src/deploy.sh` is exactly 1.
    - `grep -c '^source \.env$' src/deploy.sh` is 0 (no bare source .env remaining).
    - `grep -c 'scp \.env ' src/deploy.sh` is 0 (scp uses ../.env now).
    - `grep -c 'TEMPLATE="amnezia.key.template.txt"' src/deploy.sh` is 0 (old path gone).
  </done>
</task>

<task type="auto">
  <name>Task 3: Update .gitignore for src/ path prefix</name>
  <files>.gitignore</files>
  <read_first>
    - .gitignore — current contents; two entries reference configs/ paths that need updating.
  </read_first>
  <action>
    Update two gitignore entries to reflect the new src/ prefix:
    - `configs/white-list-extended.txt` → `src/configs/white-list-extended.txt`
    - `configs/ru-exclude.txt` → `src/configs/ru-exclude.txt`

    Do not change any other entries. Do not change `.env.secrets`, `__pycache__/`, `*.pyc`, `/.idea/`, `/.claude/`, `/awg0.conf`.
  </action>
  <verify>
    <automated>grep -q 'src/configs/white-list-extended.txt' .gitignore && grep -q 'src/configs/ru-exclude.txt' .gitignore && ! grep -qE '^configs/white-list-extended\.txt$' .gitignore && ! grep -qE '^configs/ru-exclude\.txt$' .gitignore</automated>
  </verify>
  <done>
    - `grep -c 'src/configs/white-list-extended.txt' .gitignore` is exactly 1.
    - `grep -c 'src/configs/ru-exclude.txt' .gitignore` is exactly 1.
    - `grep -c '^configs/white-list-extended\.txt$' .gitignore` is 0.
    - `grep -c '^configs/ru-exclude\.txt$' .gitignore` is 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git mv rename tracking | git mv preserves rename history; plain mv would lose file-level history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-00 | Tampering | .env / .env.secrets path change in deploy.sh | mitigate | New path is `../.env` relative to src/ — resolves to same file at repo root; preflight checks guard missing file with exit 1 |
| T-10-00b | Denial of Service | deploy.sh breakage from cd approach | mitigate | `bash -n src/deploy.sh` gate in Task 2 <done> verifies syntax; cd to own dir is a standard bash pattern |
</threat_model>

<verification>
- `test -d src/scripts && test -d src/configs && test -d src/systemd && test -d src/tests && test -f src/deploy.sh` — all pass.
- `! test -d scripts && ! test -d configs && ! test -d systemd && ! test -f deploy.sh` — source dirs gone from root.
- `bash -n src/deploy.sh` exits 0.
- `grep -q 'cd "$(dirname "${BASH_SOURCE\[0\]}")"' src/deploy.sh` passes.
- `grep -q 'src/configs/white-list-extended.txt' .gitignore` passes.
- `git status --short | grep -cE '^R'` is at least 6 (all renames staged in git).
</verification>

<success_criteria>
1. All source directories live under `src/` and their old locations at repo root are gone.
2. `src/deploy.sh` auto-navigates to its own directory at startup — no *_LOCAL var changes needed.
3. `.gitignore` updated to use `src/configs/` prefix.
4. Wave 1 plans (10-01, 10-02) can now reference files at `src/scripts/`, `src/configs/`, `src/deploy.sh`.
</success_criteria>

<output>
Create `.planning/phases/10-splitgate-ergonomics/10-00-SUMMARY.md` when done.
</output>
