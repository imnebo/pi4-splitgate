---
phase: 10-splitgate-ergonomics
plan: 01
type: execute
wave: 1
depends_on:
  - 10-00
files_modified:
  - src/scripts/routing.sh
  - src/scripts/vpn-status.sh
  - src/scripts/vpn-rollback.sh
  - src/scripts/update-vpn-routes
  - src/scripts/watch-routes.py
  - src/scripts/10-vpn-routes
  - src/systemd/vpn-routing.service
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every script that runs on the RPi sources /etc/splitgate/vpn-gateway.env (not /etc/vpn-gateway.env)"
    - "routing.sh uses /etc/splitgate/white-list.txt and /etc/splitgate/white-list-extended.txt for its data files"
    - "update-vpn-routes calls /etc/splitgate/routing.sh (not /etc/routing.sh) for recovery and post-update activation"
    - "vpn-status.sh and watch-routes.py invoke /etc/splitgate/asn-lookup.py (not /etc/asn-lookup.py)"
    - "vpn-rollback.sh teardown removes /usr/local/bin/splitgate and rm -rf /etc/splitgate/ as final filesystem steps (D-18)"
    - "systemd/vpn-routing.service ExecStart points to /etc/splitgate/routing.sh"
    - "scripts/10-vpn-routes (NM dispatcher) calls /etc/splitgate/routing.sh --no-update"
    - "IPTABLES_RULES still equals /etc/iptables/rules.v4 in routing.sh (D-12: iptables-persistent path stays)"
  artifacts:
    - path: "src/scripts/routing.sh"
      provides: "Split-tunnel routing script with /etc/splitgate/ data + env paths"
      contains: "WHITE_LIST_FILE=\"/etc/splitgate/white-list.txt\""
    - path: "src/scripts/vpn-status.sh"
      provides: "Status tool that sources env and asn-lookup from /etc/splitgate/"
      contains: "/etc/splitgate/vpn-gateway.env"
    - path: "src/scripts/vpn-rollback.sh"
      provides: "Teardown that removes dispatcher + /etc/splitgate/ (D-18)"
      contains: "rm -rf /etc/splitgate"
    - path: "src/scripts/update-vpn-routes"
      provides: "Cron updater that invokes /etc/splitgate/routing.sh"
      contains: "/etc/splitgate/routing.sh --no-update"
    - path: "src/scripts/watch-routes.py"
      provides: "Watcher using /etc/splitgate/asn-lookup.py"
      contains: "ASN_LOOKUP_PATH = \"/etc/splitgate/asn-lookup.py\""
    - path: "src/scripts/10-vpn-routes"
      provides: "NM dispatcher calling /etc/splitgate/routing.sh"
      contains: "/etc/splitgate/routing.sh --no-update"
    - path: "src/systemd/vpn-routing.service"
      provides: "Service unit pointing ExecStart at /etc/splitgate/routing.sh"
      contains: "ExecStart=/etc/splitgate/routing.sh"
  key_links:
    - from: "src/scripts/routing.sh"
      to: "/etc/splitgate/vpn-gateway.env"
      via: "guard + source line"
      pattern: "source /etc/splitgate/vpn-gateway.env"
    - from: "src/scripts/update-vpn-routes"
      to: "/etc/splitgate/routing.sh"
      via: "post-update + recovery invocation"
      pattern: "/etc/splitgate/routing.sh --no-update"
    - from: "src/scripts/vpn-status.sh"
      to: "/etc/splitgate/asn-lookup.py"
      via: "python3 invocation for ASN lookup"
      pattern: "python3 /etc/splitgate/asn-lookup.py"
    - from: "src/scripts/vpn-rollback.sh"
      to: "/etc/splitgate/ and /usr/local/bin/splitgate"
      via: "final teardown step (D-18)"
      pattern: "rm -rf /etc/splitgate"
    - from: "src/systemd/vpn-routing.service"
      to: "/etc/splitgate/routing.sh"
      via: "ExecStart"
      pattern: "ExecStart=/etc/splitgate/routing.sh"
---

<objective>
Migrate every internal `/etc/` path reference in the on-RPi scripts and the systemd unit to the new `/etc/splitgate/` namespace (D-01, D-03, D-04). Replace `/etc/vpn-gateway.env`, `/etc/white-list.txt`, `/etc/white-list-extended.txt`, `/etc/ru-exclude.txt`, `/etc/routing.sh`, `/etc/asn-lookup.py` everywhere they appear in scripts. Extend `vpn-rollback.sh` per D-18 to remove `/usr/local/bin/splitgate` and the entire `/etc/splitgate/` tree at the end of teardown. Leave `/etc/iptables/rules.v4` unchanged (D-12) and leave `/etc/cron.d/vpn-routes` cleanup unchanged (D-06).

Purpose: All scripts must find each other and their data files at the new namespaced path after Phase 10 deploy. This plan touches the scripts themselves; Plan 02 updates deploy.sh remote variables that put them at the new path.

Output: Modified src/scripts/routing.sh, src/scripts/vpn-status.sh, src/scripts/vpn-rollback.sh, src/scripts/update-vpn-routes, src/scripts/watch-routes.py, src/scripts/10-vpn-routes, src/systemd/vpn-routing.service — committed.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-splitgate-ergonomics/10-CONTEXT.md
@.planning/phases/10-splitgate-ergonomics/10-RESEARCH.md

<!-- Source-of-truth tables for every line that changes are in 10-RESEARCH.md
     section "Internal Path Reference Changes Required". Each script's table
     gives line numbers and old → new values. Read the section before editing. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /etc/ paths in routing.sh, vpn-status.sh, update-vpn-routes, watch-routes.py</name>
  <files>src/scripts/routing.sh, src/scripts/vpn-status.sh, src/scripts/update-vpn-routes, src/scripts/watch-routes.py</files>
  <read_first>
    - src/scripts/routing.sh — current path constants and env guard block (see 10-RESEARCH.md table for routing.sh: lines 38, 39, 41, 63–64, 68, 70)
    - src/scripts/vpn-status.sh — env guard, source line, and asn-lookup invocation (10-RESEARCH.md table: lines 40–41, 45, 194)
    - src/scripts/update-vpn-routes — SUBNET_FILE, env guard, source line, EXCLUDE_FILE, post-update routing.sh calls (10-RESEARCH.md table: lines 19, 30–31, 35, 41, 65, 95)
    - src/scripts/watch-routes.py — ASN_LOOKUP_PATH constant (10-RESEARCH.md table: line 46)
    - .planning/phases/10-splitgate-ergonomics/10-RESEARCH.md — section "Internal Path Reference Changes Required" (the per-file change tables)
    - .planning/phases/10-splitgate-ergonomics/10-CONTEXT.md — D-01..D-05 (the namespace), D-12 (iptables-persistent path stays)
  </read_first>
  <action>
    Update every `/etc/`-rooted reference inside these four scripts to the `/etc/splitgate/` namespace, exactly per the per-script tables in 10-RESEARCH.md. Concrete target values:

    scripts/routing.sh —
    - `WHITE_LIST_FILE="/etc/splitgate/white-list.txt"` (replaces `/etc/white-list.txt`)
    - `EXCEPTIONS_FILE="/etc/splitgate/white-list-extended.txt"` (replaces `/etc/white-list-extended.txt`)
    - Env guard + source: every occurrence of `/etc/vpn-gateway.env` becomes `/etc/splitgate/vpn-gateway.env` (existence check, error message, source line, log message)
    - DO NOT change `IPTABLES_RULES="/etc/iptables/rules.v4"` — D-12 keeps the iptables-persistent path
    - Header comments at the top of the file that document the deploy target (`Deployed to /etc/routing.sh`) update to `Deployed to /etc/splitgate/routing.sh` for accuracy
    - Comments mentioning `/etc/white-list.txt`, `/etc/white-list-extended.txt`, `/etc/vpn-gateway.env` get updated to the new paths

    scripts/vpn-status.sh —
    - Every `/etc/vpn-gateway.env` (guard test, error message, `source` line) → `/etc/splitgate/vpn-gateway.env`
    - The asn-lookup detection block: replace both `/etc/asn-lookup.py` references (the `[[ -f /etc/asn-lookup.py ]]` test and the `python3 /etc/asn-lookup.py` invocation) with `/etc/splitgate/asn-lookup.py`
    - Header comments that reference deploy path `/etc/vpn-status.sh` update to `/etc/splitgate/vpn-status.sh`

    scripts/update-vpn-routes —
    - `SUBNET_FILE="/etc/splitgate/white-list.txt"` (replaces `/etc/white-list.txt`)
    - Env guard + error + source: every `/etc/vpn-gateway.env` → `/etc/splitgate/vpn-gateway.env`
    - `EXCLUDE_FILE="/etc/splitgate/ru-exclude.txt"` (replaces `/etc/ru-exclude.txt`)
    - Both `/etc/routing.sh --no-update` invocations (current lines 65 and 95) → `/etc/splitgate/routing.sh --no-update`
    - Header comments mentioning deploy target `/etc/update-vpn-routes` update to `/etc/splitgate/update-vpn-routes`; comments naming `/etc/white-list.txt` or `/etc/routing.sh` update to the splitgate path

    scripts/watch-routes.py —
    - `ASN_LOOKUP_PATH = "/etc/splitgate/asn-lookup.py"` (replaces `/etc/asn-lookup.py`)
    - Module-docstring lines that mention `/etc/asn-lookup.py` update to the new path

    Do NOT introduce new helpers, refactor logic, or change behavior — this is a pure path migration. Preserve existing function bodies, control flow, comments, shebangs, and `set -euo pipefail` lines. Do not touch logging/output style.
  </action>
  <verify>
    <automated>grep -nE "/etc/(vpn-gateway\.env|white-list(-extended)?\.txt|ru-exclude\.txt|asn-lookup\.py|routing\.sh|update-vpn-routes|vpn-status\.sh|watch-routes\.py)" src/scripts/routing.sh src/scripts/vpn-status.sh src/scripts/update-vpn-routes src/scripts/watch-routes.py | grep -v '/etc/splitgate/' | grep -v '^[^:]*:[[:space:]]*#' | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    - Above grep (non-comment, non-splitgate hits across the four files) prints `0`.
    - `grep -n "WHITE_LIST_FILE=\"/etc/splitgate/white-list.txt\"" src/scripts/routing.sh` returns exactly 1 line.
    - `grep -n "EXCEPTIONS_FILE=\"/etc/splitgate/white-list-extended.txt\"" src/scripts/routing.sh` returns exactly 1 line.
    - `grep -n "IPTABLES_RULES=\"/etc/iptables/rules.v4\"" src/scripts/routing.sh` still returns 1 line (D-12 untouched).
    - `grep -n "SUBNET_FILE=\"/etc/splitgate/white-list.txt\"" src/scripts/update-vpn-routes` returns 1 line.
    - `grep -n "EXCLUDE_FILE=\"/etc/splitgate/ru-exclude.txt\"" src/scripts/update-vpn-routes` returns 1 line.
    - `grep -c "/etc/splitgate/routing.sh --no-update" src/scripts/update-vpn-routes` is exactly `2`.
    - `grep -n 'ASN_LOOKUP_PATH = "/etc/splitgate/asn-lookup.py"' src/scripts/watch-routes.py` returns 1 line.
    - `grep -c "python3 /etc/splitgate/asn-lookup.py" src/scripts/vpn-status.sh` is exactly `1`.
    - `bash -n src/scripts/routing.sh && bash -n src/scripts/vpn-status.sh && bash -n src/scripts/update-vpn-routes` exits 0 (syntax check).
    - `python3 -m py_compile src/scripts/watch-routes.py` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite /etc/ paths in vpn-rollback.sh, 10-vpn-routes, and systemd/vpn-routing.service; extend rollback teardown per D-18</name>
  <files>src/scripts/vpn-rollback.sh, src/scripts/10-vpn-routes, src/systemd/vpn-routing.service</files>
  <read_first>
    - src/scripts/vpn-rollback.sh — current env guard (lines 37–38), source line (42), white-list-extended removal block (109–111), cron rm (122), final summary echoes (144–152). The "Step 8 Final state summary" block lists Preserved paths that must reflect the new behavior.
    - src/scripts/10-vpn-routes — current ExecStart-style call at line 17 invoking `/etc/routing.sh --no-update`
    - src/systemd/vpn-routing.service — ExecStart line (20) currently `/etc/routing.sh`
    - .planning/phases/10-splitgate-ergonomics/10-CONTEXT.md — D-18 (rollback removes `/usr/local/bin/splitgate` and `/etc/splitgate/`), D-09 (NM dispatcher file stays at its system path but its body must call the new script path), D-10 (systemd unit file stays at its system path but ExecStart must point at the new script path)
    - .planning/phases/10-splitgate-ergonomics/10-RESEARCH.md — sections "scripts/vpn-rollback.sh", "scripts/10-vpn-routes", "systemd/vpn-routing.service" change tables; Pitfall 5 (rollback rm placement) and Pitfall 7 (white-list.txt no longer preserved)
  </read_first>
  <action>
    scripts/vpn-rollback.sh:
    - Env guard + error message + source line: replace every `/etc/vpn-gateway.env` with `/etc/splitgate/vpn-gateway.env`.
    - Remove the current Step 4c block that does `rm -f /etc/white-list-extended.txt` (lines ~107–111). It is now redundant — the entire `/etc/splitgate/` tree is removed below.
    - Keep all existing teardown steps in place (iptables LOG rule removal, netfilter-persistent save, cron entry removal at `/etc/cron.d/vpn-routes` which D-06 keeps unchanged, default route restoration).
    - After the existing Step 7 "Restore ISP default route" and BEFORE the Step 8 "Final state summary" block, add a new step "Step 7b: Remove splitgate dispatcher and /etc/splitgate/ tree (D-18)" that runs, in order: `rm -f /usr/local/bin/splitgate` then `rm -rf /etc/splitgate`. Each command followed by a `log` line announcing the removal. Use the same `log` helper already defined in the script. Placement is the LAST filesystem step before the summary (Pitfall 5: rm comes after route restoration so a failure mid-teardown still leaves the rollback script callable).
    - Update the "Step 8 Final state summary" block:
      * Remove the line `echo "   Exception file removed: /etc/white-list-extended.txt (if present)"`
      * Replace it with two lines under a new "Removed (D-18 splitgate teardown):" heading: `echo "   /usr/local/bin/splitgate"` and `echo "   /etc/splitgate/ (entire tree, including white-list.txt, white-list-extended.txt, ru-exclude.txt, vpn-gateway.env)"`
      * Update the existing "Preserved (not removed):" block — DELETE the `/etc/routing.sh` and `/etc/white-list.txt` lines (those paths no longer exist) and DELETE the `/etc/vpn-gateway.env` reference if present. Keep `/etc/amnezia/amneziawg/awg0.conf` and `AmneziaWG packages`. Also keep the "Note: /etc/dnsmasq.conf and /etc/vpn-status.sh remain on disk" line BUT change `/etc/vpn-status.sh` → `/etc/splitgate/vpn-status.sh` is wrong because by this point /etc/splitgate is gone — replace the note with: `Note: /etc/dnsmasq.conf remains on disk (system file at /etc/, not removed)`.
    - Header comment block at top of file: update the "Preserved files" decision note (D-10 comment) to reflect the new D-18 behavior (`/etc/splitgate/` removed entirely; `/etc/amnezia/amneziawg/awg0.conf` and AmneziaWG packages preserved).
    - The `Usage: sudo /etc/vpn-rollback.sh` header comment becomes `Usage: sudo /etc/splitgate/vpn-rollback.sh`.

    scripts/10-vpn-routes:
    - At line 17, replace `/etc/routing.sh --no-update >> /var/log/vpn-routes.log 2>&1 &` with `/etc/splitgate/routing.sh --no-update >> /var/log/vpn-routes.log 2>&1 &`.
    - Leave the `/var/log/vpn-routes.log` redirect target untouched — it is the existing cron-style log file used by the NM dispatcher and is unrelated to the Phase 9 centralized log (per 10-RESEARCH.md note under 10-vpn-routes table).
    - Header comments referencing the old `/etc/routing.sh` path get updated to `/etc/splitgate/routing.sh`.

    systemd/vpn-routing.service:
    - At line 20, change `ExecStart=/etc/routing.sh` to `ExecStart=/etc/splitgate/routing.sh`.
    - Update the D-01 comment block at the top of the file (line 3) so the inline annotation references the new path.

    Do NOT change any other text. Do NOT touch the unit's `After=`/`Wants=`/`Type=`/install ordering — only the `ExecStart` value.
  </action>
  <verify>
    <automated>bash -n src/scripts/vpn-rollback.sh && grep -q "^rm -rf /etc/splitgate" src/scripts/vpn-rollback.sh && grep -q "^rm -f /usr/local/bin/splitgate" src/scripts/vpn-rollback.sh && grep -q "/etc/splitgate/routing.sh --no-update" src/scripts/10-vpn-routes && grep -q "^ExecStart=/etc/splitgate/routing.sh$" src/systemd/vpn-routing.service</automated>
  </verify>
  <done>
    - `bash -n src/scripts/vpn-rollback.sh` exits 0.
    - `grep -c "^rm -rf /etc/splitgate" src/scripts/vpn-rollback.sh` is exactly `1`.
    - `grep -c "^rm -f /usr/local/bin/splitgate" src/scripts/vpn-rollback.sh` is exactly `1`.
    - In `src/scripts/vpn-rollback.sh`, the line `rm -f /usr/local/bin/splitgate` appears BEFORE the line `rm -rf /etc/splitgate`, and BOTH appear AFTER the `ip route add default via "${KEENETIC_GW}"` line and BEFORE the final `================================================================` summary banner. Verified via:
      `awk '/ip route add default via/{a=NR} /rm -f \/usr\/local\/bin\/splitgate/{b=NR} /rm -rf \/etc\/splitgate/{c=NR} /VPN gateway rollback complete/{d=NR} END{exit !(a<b && b<c && c<d)}' src/scripts/vpn-rollback.sh`
    - `grep -c '/etc/white-list-extended.txt' src/scripts/vpn-rollback.sh` is `0` (block removed; namespace cleanup handles it).
    - `grep -c "/etc/splitgate/routing.sh --no-update" src/scripts/10-vpn-routes` is exactly `1`.
    - `grep -c '^/etc/routing.sh' src/scripts/10-vpn-routes` is `0`.
    - `grep -c '^ExecStart=/etc/splitgate/routing.sh$' src/systemd/vpn-routing.service` is exactly `1`.
    - `grep -c '^ExecStart=/etc/routing.sh$' src/systemd/vpn-routing.service` is `0`.
    - Manual smoke check: a fresh read of src/scripts/vpn-rollback.sh shows the new D-18 teardown step exists with `log` lines announcing each removal.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → rollback execution | operator triggers `sudo vpn-rollback.sh` with destructive privileges (rm -rf root paths) |
| systemd → routing.sh exec | systemd boots the unit at root; ExecStart path is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | scripts/vpn-rollback.sh `rm -rf /etc/splitgate` | mitigate | Hardcoded literal path, no variable expansion: `rm -rf /etc/splitgate` (not `rm -rf "$VAR"`); placed only as the last filesystem step (Pitfall 5) so prior teardown completes first |
| T-10-02 | Denial of Service | systemd/vpn-routing.service ExecStart pointing at missing path | mitigate | After Phase 10 deploy completes (Plan 02 + 03), service file SCP + `systemctl daemon-reload` re-runs; Stage 13 in deploy.sh already issues daemon-reload — verified by reading deploy.sh lines 312–315 |
| T-10-03 | Information Disclosure | exposing env-file path in error messages | accept | error messages name the new path `/etc/splitgate/vpn-gateway.env`; the env file contains non-secret config (per RESEARCH security domain); secrets remain in `.env.secrets` on macOS |
</threat_model>

<verification>
- All seven changed files pass syntax check (`bash -n` for shell scripts, `python3 -m py_compile` for Python).
- `grep -nE "/etc/(vpn-gateway\.env|white-list(-extended)?\.txt|ru-exclude\.txt|asn-lookup\.py|routing\.sh)" src/scripts/*.sh src/scripts/*.py src/scripts/10-vpn-routes src/systemd/vpn-routing.service` shows only `/etc/splitgate/...` matches (the only allowed unchanged `/etc/...` paths are `/etc/iptables/rules.v4`, `/etc/cron.d/vpn-routes`, `/etc/amnezia/amneziawg/awg0.conf`, `/etc/NetworkManager/dispatcher.d/...`, `/etc/dnsmasq.conf`).
- Comment blocks that document the deploy target path inside each script reference the new `/etc/splitgate/...` path.
</verification>

<success_criteria>
1. Every internal path in scripts/routing.sh, scripts/vpn-status.sh, scripts/vpn-rollback.sh, scripts/update-vpn-routes, scripts/watch-routes.py, scripts/10-vpn-routes, and systemd/vpn-routing.service that previously read `/etc/foo` (where foo is one of the migrated files) now reads `/etc/splitgate/foo`. Verified by the grep gates in Task 1 and Task 2.
2. `/etc/iptables/rules.v4` (D-12) and `/etc/cron.d/vpn-routes` (D-06) references remain unchanged.
3. vpn-rollback.sh removes `/usr/local/bin/splitgate` then `/etc/splitgate/` as the last filesystem operations before the summary banner (D-18; Pitfall 5 ordering).
4. systemd/vpn-routing.service ExecStart = `/etc/splitgate/routing.sh`.
5. scripts/10-vpn-routes calls `/etc/splitgate/routing.sh --no-update`.
6. All scripts pass `bash -n` / `python3 -m py_compile`.
</success_criteria>

<output>
Create `.planning/phases/10-splitgate-ergonomics/10-01-SUMMARY.md` when done.
</output>
