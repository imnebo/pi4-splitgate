---
phase: 10-splitgate-ergonomics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy.sh
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every *_REMOTE variable in deploy.sh that points to a Phase-10-migrated file resolves to a path under /etc/splitgate/"
    - "deploy.sh runs `sudo mkdir -p /etc/splitgate/logs` before deploying any file under /etc/splitgate/ (Pitfall 4)"
    - "deploy.sh deploys the splitgate dispatcher to /usr/local/bin/splitgate (chmod +x, root:root)"
    - "deploy.sh deploys configs/logrotate-vpn-gateway to /etc/logrotate.d/vpn-gateway (mode 644, root:root)"
    - "deploy.sh declares SPLITGATE_DISPATCHER_LOCAL=scripts/splitgate and LOGROTATE_CONF_LOCAL=configs/logrotate-vpn-gateway and the preflight checks both exist"
    - "TOTAL_STAGES matches the actual count of `[N/${TOTAL_STAGES}]` stage echoes (Pitfall 6)"
    - "Stages that activate routing.sh (current Stage 11 and Stage 24) reference ${ROUTING_SH_REMOTE} so the new /etc/splitgate/routing.sh path is used automatically"
  artifacts:
    - path: "deploy.sh"
      provides: "Deploy orchestrator with splitgate namespace remote paths and new mkdir / dispatcher / logrotate stages"
      contains: "SPLITGATE_DIR_REMOTE=\"/etc/splitgate\""
  key_links:
    - from: "deploy.sh"
      to: "/etc/splitgate/ on RPi"
      via: "all *_REMOTE variables for migrated files"
      pattern: "_REMOTE=\"/etc/splitgate/"
    - from: "deploy.sh new mkdir stage"
      to: "all file-deploy stages"
      via: "stage ordering — mkdir runs before any sudo mv into /etc/splitgate/"
      pattern: "mkdir -p /etc/splitgate"
    - from: "deploy.sh dispatcher stage"
      to: "/usr/local/bin/splitgate"
      via: "scp scripts/splitgate → /tmp → sudo mv + chmod +x"
      pattern: "/usr/local/bin/splitgate"
---

<objective>
Update `deploy.sh` so every Phase-10-migrated `*_REMOTE` variable points under `/etc/splitgate/`, add the new variables for the splitgate dispatcher and logrotate config, add a preflight existence check for both new local files (scripts/splitgate, configs/logrotate-vpn-gateway), insert three new deploy stages (mkdir `/etc/splitgate/logs/`; deploy splitgate dispatcher to `/usr/local/bin/splitgate`; deploy `configs/logrotate-vpn-gateway` to `/etc/logrotate.d/vpn-gateway`), and bump `TOTAL_STAGES` to the new count. All inline file-deploy stage bodies continue to reference `${*_REMOTE}` variables (no inline path strings) so variable updates propagate to every existing stage automatically (per 10-RESEARCH.md "Pattern: deploy.sh variable-driven stages").

Purpose: Without these deploy.sh changes the new `/etc/splitgate/` namespace would never exist on the RPi and the dispatcher + logrotate config would never reach the filesystem. Plan 03 creates the new artifact files (`scripts/splitgate` and `configs/logrotate-vpn-gateway`) — but the preflight check additions here intentionally point at those filenames so the deploy fails fast if Plan 03 hasn't run. This is correct: Plan 03 must complete before any actual deploy invocation.

Output: Modified deploy.sh with the new variables, new preflight checks, new stages, updated TOTAL_STAGES, and updated final summary lines.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-splitgate-ergonomics/10-CONTEXT.md
@.planning/phases/10-splitgate-ergonomics/10-RESEARCH.md

<!-- 10-RESEARCH.md section "deploy.sh Changes Required" gives the exact list
     of *_REMOTE variables to update and the new variables to add. The
     "Architecture Patterns: deploy.sh variable-driven stages" note explains why
     updating the variable suffices for every stage body. -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update *_REMOTE variables and add new SPLITGATE/LOGROTATE variables in deploy.sh</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh — variable declarations block (lines 32–72), preflight existence checks (101–158), and the final summary echo block (lines 402–503). The current TOTAL_STAGES is `24` at line 72.
    - .planning/phases/10-splitgate-ergonomics/10-RESEARCH.md — section "deploy.sh Changes Required" → subsection "Remote path variable updates" (the complete old→new table) and "New variables to add" (the eight new SPLITGATE_/LOGROTATE_ variables).
    - .planning/phases/10-splitgate-ergonomics/10-CONTEXT.md — D-01 (`/etc/splitgate/`), D-02 (`/etc/splitgate/logs/`), D-06..D-12 (paths that stay unchanged), D-13 (dispatcher at `/usr/local/bin/splitgate`), D-17 (deploy stage for dispatcher).
  </read_first>
  <action>
    Update existing `*_REMOTE` variables in the declarations block to point under `/etc/splitgate/`. Concrete target values (in declaration order; do NOT renumber declarations, just edit values):

    - `ENV_REMOTE="/etc/splitgate/vpn-gateway.env"` (was `/etc/vpn-gateway.env`)
    - `ROUTING_SH_REMOTE="/etc/splitgate/routing.sh"` (was `/etc/routing.sh`)
    - `UPDATE_VPN_ROUTES_REMOTE="/etc/splitgate/update-vpn-routes"` (was `/etc/update-vpn-routes`)
    - `VPN_ROLLBACK_REMOTE="/etc/splitgate/vpn-rollback.sh"` (was `/etc/vpn-rollback.sh`)
    - `VPN_STATUS_REMOTE="/etc/splitgate/vpn-status.sh"` (was `/etc/vpn-status.sh`)
    - `WATCH_ROUTES_REMOTE="/etc/splitgate/watch-routes.py"` (was `/etc/watch-routes.py`)
    - `ASN_LOOKUP_REMOTE="/etc/splitgate/asn-lookup.py"` (was `/etc/asn-lookup.py`)
    - `WHITE_LIST_EXT_REMOTE="/etc/splitgate/white-list-extended.txt"` (was `/etc/white-list-extended.txt`)
    - `EXCLUDE_LIST_REMOTE="/etc/splitgate/ru-exclude.txt"` (was `/etc/ru-exclude.txt`)

    DO NOT change any of these (D-06..D-12, D-04 keys):
    - `AWG_CONF_REMOTE="/etc/amnezia/amneziawg/awg0.conf"`
    - `VPN_ROUTING_SERVICE_REMOTE="/etc/systemd/system/vpn-routing.service"`
    - `CRON_FILE_REMOTE="/etc/cron.d/vpn-routes"`
    - `DNSMASQ_CONF_REMOTE="/etc/dnsmasq.conf"`
    - `NM_DISPATCHER_REMOTE="/etc/NetworkManager/dispatcher.d/10-vpn-routes"`
    - All `*_TMP` variables (these point under `/tmp/` and stay as-is)
    - All `*_LOCAL` variables for repo-side paths (stay as-is)

    Add the following NEW variables at the end of the declarations block, immediately after `NM_DISPATCHER_TMP` (line 70) and BEFORE `TOTAL_STAGES=`:
    - `SPLITGATE_DIR_REMOTE="/etc/splitgate"`
    - `SPLITGATE_LOGS_REMOTE="/etc/splitgate/logs"`
    - `SPLITGATE_DISPATCHER_LOCAL="scripts/splitgate"`
    - `SPLITGATE_DISPATCHER_REMOTE="/usr/local/bin/splitgate"`
    - `SPLITGATE_DISPATCHER_TMP="/tmp/splitgate.tmp"`
    - `LOGROTATE_CONF_LOCAL="configs/logrotate-vpn-gateway"`
    - `LOGROTATE_CONF_REMOTE="/etc/logrotate.d/vpn-gateway"`
    - `LOGROTATE_CONF_TMP="/tmp/logrotate-vpn-gateway.tmp"`

    Bump `TOTAL_STAGES=24` to `TOTAL_STAGES=27` (Stage 25 = mkdir, Stage 26 = dispatcher deploy, Stage 27 = logrotate deploy — added by Task 2 of this plan).

    Add two new preflight existence checks in the Stage 1 block (after the existing `NM_DISPATCHER_LOCAL` check at line 153–156): one for `${SPLITGATE_DISPATCHER_LOCAL}` and one for `${LOGROTATE_CONF_LOCAL}`. Each follows the existing pattern: `if [[ ! -f "$VAR" ]]; then echo "ERROR: $VAR not found — run from the repo root" >&2; exit 1; fi`.

    Update the header comment block at the top of the file (lines 20–28: "This script deploys:") so the listed paths reflect the new `/etc/splitgate/...` namespace. Add two new bullet lines: "splitgate dispatcher → /usr/local/bin/splitgate" and "logrotate config → /etc/logrotate.d/vpn-gateway".

    DO NOT rewrite any existing stage body (Stages 2–24). The stage bodies all reference `${*_REMOTE}` variables, so changing the variable values is sufficient to migrate every file-deploy and activation stage to the new paths. Verify this by reading each ssh/scp line in Stages 6, 7, 10, 11, 12, 14, 15, 16, 18, 19, 20, 21, 21b, 22, 23, 24 — they all use `${VAR}` interpolation, not inline `/etc/...` strings.

    EXCEPTION — final summary block (lines ~402–503): some of the `echo` lines hardcode literal old paths (e.g. `ssh pi4 "sudo /etc/routing.sh"` and `ssh pi4 "sudo /etc/vpn-status.sh ..."` and `ssh pi4 "sudo /etc/vpn-rollback.sh"`). Replace each hardcoded literal old path in echoed user-instructions with the corresponding `${*_REMOTE}` variable (NOT a new literal), so updating the variable in the future continues to propagate. Specifically:
    - `sudo /etc/routing.sh` (echoed) → `sudo \${ROUTING_SH_REMOTE}` form, or rewrite the message as `ssh pi4 "sudo /etc/splitgate/routing.sh"` literal — use the literal new path (matches the existing style of mixing literals and variables in summaries).
    - `sudo /etc/update-vpn-routes` (echoed) → `sudo /etc/splitgate/update-vpn-routes` literal.
    - `sudo /etc/vpn-status.sh` (echoed) → `sudo /etc/splitgate/vpn-status.sh` literal.
    - `sudo /etc/vpn-rollback.sh` (echoed) → `sudo /etc/splitgate/vpn-rollback.sh` literal.
    - Phase 3 cron verification expected line: `0 ${CRON_UPDATE_HOUR} * * * root /etc/update-vpn-routes ...` → `0 ${CRON_UPDATE_HOUR} * * * root /etc/splitgate/update-vpn-routes >> /var/log/vpn-routes.log 2>&1`.
    - Phase 5 verification: `ls -l /etc/white-list-extended.txt` → `ls -l /etc/splitgate/white-list-extended.txt`.
    - The "Phase 4 verification" line `sudo /etc/vpn-status.sh` and the idempotency check `sudo /etc/routing.sh && sudo iptables -L FORWARD -n -v | grep -c LOG` get their paths updated to `/etc/splitgate/...` likewise.
  </action>
  <verify>
    <automated>bash -n deploy.sh && grep -q '^TOTAL_STAGES=27$' deploy.sh && grep -q '^SPLITGATE_DIR_REMOTE="/etc/splitgate"$' deploy.sh && grep -q '^SPLITGATE_DISPATCHER_REMOTE="/usr/local/bin/splitgate"$' deploy.sh && grep -q '^LOGROTATE_CONF_REMOTE="/etc/logrotate.d/vpn-gateway"$' deploy.sh</automated>
  </verify>
  <done>
    - `bash -n deploy.sh` exits 0.
    - `grep -c '^TOTAL_STAGES=27$' deploy.sh` is exactly `1`.
    - The following each return exactly 1: `grep -c '^ENV_REMOTE="/etc/splitgate/vpn-gateway.env"$' deploy.sh`, `grep -c '^ROUTING_SH_REMOTE="/etc/splitgate/routing.sh"$' deploy.sh`, `grep -c '^UPDATE_VPN_ROUTES_REMOTE="/etc/splitgate/update-vpn-routes"$' deploy.sh`, `grep -c '^VPN_ROLLBACK_REMOTE="/etc/splitgate/vpn-rollback.sh"$' deploy.sh`, `grep -c '^VPN_STATUS_REMOTE="/etc/splitgate/vpn-status.sh"$' deploy.sh`, `grep -c '^WATCH_ROUTES_REMOTE="/etc/splitgate/watch-routes.py"$' deploy.sh`, `grep -c '^ASN_LOOKUP_REMOTE="/etc/splitgate/asn-lookup.py"$' deploy.sh`, `grep -c '^WHITE_LIST_EXT_REMOTE="/etc/splitgate/white-list-extended.txt"$' deploy.sh`, `grep -c '^EXCLUDE_LIST_REMOTE="/etc/splitgate/ru-exclude.txt"$' deploy.sh`.
    - The eight new variables each appear exactly once: `SPLITGATE_DIR_REMOTE`, `SPLITGATE_LOGS_REMOTE`, `SPLITGATE_DISPATCHER_LOCAL`, `SPLITGATE_DISPATCHER_REMOTE`, `SPLITGATE_DISPATCHER_TMP`, `LOGROTATE_CONF_LOCAL`, `LOGROTATE_CONF_REMOTE`, `LOGROTATE_CONF_TMP`.
    - Variables D-06..D-12 unchanged: `grep -c '^AWG_CONF_REMOTE="/etc/amnezia/amneziawg/awg0.conf"$' deploy.sh` is 1; `grep -c '^DNSMASQ_CONF_REMOTE="/etc/dnsmasq.conf"$' deploy.sh` is 1; `grep -c '^CRON_FILE_REMOTE="/etc/cron.d/vpn-routes"$' deploy.sh` is 1.
    - Preflight checks contain both new files: `grep -c 'SPLITGATE_DISPATCHER_LOCAL' deploy.sh` is at least 2 (declaration + preflight); `grep -c 'LOGROTATE_CONF_LOCAL' deploy.sh` is at least 2.
    - Final summary block has no `sudo /etc/(routing\.sh|update-vpn-routes|vpn-status\.sh|vpn-rollback\.sh)` literals: `grep -E 'sudo /etc/(routing\.sh|update-vpn-routes|vpn-status\.sh|vpn-rollback\.sh)' deploy.sh | wc -l | tr -d ' '` returns `0`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Stages 25–27 (mkdir splitgate; deploy dispatcher; deploy logrotate config) and update Final Summary</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh — Stage 22 deploy pattern (lines 384–389) and Stage 23 deploy pattern (lines 391–395): both use `scp -o BatchMode=yes "${LOCAL}" "${SSH_HOST}:${TMP}"` followed by `ssh -o BatchMode=yes "${SSH_HOST}" "sudo mv ${TMP} ${REMOTE} && sudo chmod ... && sudo chown root:root ${REMOTE}"`. Match this pattern exactly.
    - deploy.sh — Stage 24 currently the LAST stage at lines 397–400 (routing.sh activation). New Stages 25–27 must be inserted BEFORE Stage 24 so the activation stage remains the last runtime stage. (Phase 7 precedent — D-10 from STATE.md: "routing activation remains last runtime stage".)
    - deploy.sh — Final Summary "PHASE X:" echoed bullet lines (lines ~419–426).
    - .planning/phases/10-splitgate-ergonomics/10-CONTEXT.md — D-02 (`/etc/splitgate/logs/`), D-13 (dispatcher path), D-17 (deploy stage, chmod +x, root:root), D-19 (logrotate config path).
    - .planning/phases/10-splitgate-ergonomics/10-RESEARCH.md — section "New deploy stages needed" and "Pitfall 4" (mkdir must precede file deploys into /etc/splitgate/).
  </read_first>
  <action>
    Restructure stage ordering. The current sequence ends with: Stage 22 (NM dispatcher) → Stage 23 (asn-lookup.py) → Stage 24 (routing.sh activation).

    Phase 10 requires three new stages. Insert them AFTER Stage 23 and BEFORE Stage 24, so that routing.sh activation remains the LAST runtime stage. Also note: the new mkdir stage logically needs to come BEFORE any `sudo mv ... ${ENV_REMOTE}` etc. into the new namespace, but because all the earlier file-deploy stages (Stages 6, 10, 12, 14, 16, 19, 20, 21, 21b, 22, 23) now use `${*_REMOTE}` paths that resolve to `/etc/splitgate/...`, the mkdir MUST run BEFORE those stages. The right place is therefore EARLIER — insert the mkdir stage immediately after Stage 4 (AmneziaWG install) and BEFORE Stage 5 (render awg0.conf). That guarantees `/etc/splitgate/logs/` exists before any `sudo mv /tmp/foo.tmp /etc/splitgate/foo` runs.

    Final ordering after Phase 10 must therefore be:

    1. Stage 1 preflight (already includes the two new file checks added in Task 1)
    2. Stage 2 key validation
    3. Stage 3 SSH connectivity
    4. Stage 4 AmneziaWG install
    5. NEW Stage 5 → `[5/${TOTAL_STAGES}] Creating splitgate namespace on ${SSH_HOST}...` runs `ssh ${SSH_HOST} "sudo mkdir -p ${SPLITGATE_LOGS_REMOTE} && sudo chmod 755 ${SPLITGATE_DIR_REMOTE}"`. Echo `       ${SPLITGATE_DIR_REMOTE} created (mkdir -p includes logs subdirectory; chmod 755 root:root)`.
    6. All existing file-deploy and activation stages (currently 5..23) shift up by 1 — their echoes become `[6/${TOTAL_STAGES}]` ... `[24/${TOTAL_STAGES}]`. Routing.sh activation (was Stage 24) becomes Stage 25. *Update each stage echo header to its new number.*
    7. NEW Stage 26 → dispatcher deploy AFTER routing.sh activation:
       `echo "[26/${TOTAL_STAGES}] Deploying splitgate dispatcher to ${SSH_HOST}:${SPLITGATE_DISPATCHER_REMOTE}..."`
       `scp -o BatchMode=yes "${SPLITGATE_DISPATCHER_LOCAL}" "${SSH_HOST}:${SPLITGATE_DISPATCHER_TMP}"`
       `ssh -o BatchMode=yes "${SSH_HOST}" "sudo mv ${SPLITGATE_DISPATCHER_TMP} ${SPLITGATE_DISPATCHER_REMOTE} && sudo chmod +x ${SPLITGATE_DISPATCHER_REMOTE} && sudo chown root:root ${SPLITGATE_DISPATCHER_REMOTE}"`
       `echo "       splitgate dispatcher deployed (chmod +x, root:root)."`
    8. NEW Stage 27 → logrotate config deploy:
       `echo "[27/${TOTAL_STAGES}] Deploying logrotate config to ${SSH_HOST}:${LOGROTATE_CONF_REMOTE}..."`
       `scp -o BatchMode=yes "${LOGROTATE_CONF_LOCAL}" "${SSH_HOST}:${LOGROTATE_CONF_TMP}"`
       `ssh -o BatchMode=yes "${SSH_HOST}" "sudo mv ${LOGROTATE_CONF_TMP} ${LOGROTATE_CONF_REMOTE} && sudo chmod 644 ${LOGROTATE_CONF_REMOTE} && sudo chown root:root ${LOGROTATE_CONF_REMOTE}"`
       `echo "       logrotate config deployed (mode 644, root:root)."`

    Renumbering rule: each existing stage header `[N/${TOTAL_STAGES}]` becomes `[N+1/${TOTAL_STAGES}]` for N ∈ {5..24}. The Stage 21b header (currently `[21/${TOTAL_STAGES}]` per Pitfall 6) is corrected to `[22b/${TOTAL_STAGES}]` after the +1 shift. The cleanest implementation is: for each `echo "[N/${TOTAL_STAGES}]"` line where N is in 5..24, increment N by 1; for Stage 21b's `echo "[21/${TOTAL_STAGES}] Deploying ru-exclude.txt"` line, change to `[22b/${TOTAL_STAGES}]`.

    Final Summary block (lines ~402–503): update the "PHASE X:" enumerated bullets so they reflect new stage numbering and add two new bullets:
    - `echo "   PHASE 10: ${SPLITGATE_DISPATCHER_REMOTE} (chmod +x, root:root — ergonomic CLI dispatcher)"`
    - `echo "   PHASE 10: ${LOGROTATE_CONF_REMOTE} (mode 644, root:root — log rotation config for /etc/splitgate/logs/vpn-gateway.log)"`
    Append a "Phase 10 verification" block at the end (after the Phase 5 verification block) with these lines (literal text, not variable expansions for shell-clarity):
    - `ssh pi4 "ls -ld /etc/splitgate /etc/splitgate/logs"`  expect both directories present, mode 755
    - `ssh pi4 "ls -l /usr/local/bin/splitgate"`  expect `-rwxr-xr-x root root`
    - `ssh pi4 "splitgate status"` should exec `sudo /etc/splitgate/vpn-status.sh` (verify usage line if no args)
    - `ssh pi4 "splitgate"`  expect usage line `Usage: splitgate {status|watch|rollback|routing|update} [args...]` and exit 1
    - `ssh pi4 "ls -l /etc/logrotate.d/vpn-gateway"`  expect mode 644 root:root
    - `ssh pi4 "sudo logrotate -d /etc/logrotate.d/vpn-gateway"`  dry-run must exit 0

    Update the "Phase X+ deploy successful" banner header line (currently "Phase 1 + 2 + 3 (autostart + cron + rollback) deploy successful.") to add "+ Phase 10 (splitgate ergonomics)".

    DO NOT modify Stage 22 (Stage 23 after renumber) — the NM dispatcher deploy — beyond the header echo number. DO NOT change the cron line content in Stage 15 (Stage 16 after renumber); the cron line uses `${UPDATE_VPN_ROUTES_REMOTE}` so the variable update from Task 1 already propagates.
  </action>
  <verify>
    <automated>bash -n deploy.sh && N=$(grep -cE '^echo "\[[0-9]+(b)?/\$\{TOTAL_STAGES\}\]' deploy.sh) && [ "$N" -ge 27 ] && grep -q 'Creating splitgate namespace' deploy.sh && grep -q 'Deploying splitgate dispatcher' deploy.sh && grep -q 'Deploying logrotate config' deploy.sh && grep -q '/etc/splitgate/logs/vpn-gateway.log' deploy.sh</automated>
  </verify>
  <done>
    - `bash -n deploy.sh` exits 0.
    - Stage headers count: `grep -cE '^echo "\[[0-9]+(b)?/\$\{TOTAL_STAGES\}\]' deploy.sh` returns at least `27` (one per stage 1..27, plus Stage 21b/22b).
    - The new mkdir stage runs before any file-deploy into `/etc/splitgate/`. Verified by:
      `awk '/Creating splitgate namespace/{a=NR} /Deploying awg0.conf/{b=NR} END{exit !(a<b)}' deploy.sh` exits 0.
    - The dispatcher and logrotate stages run AFTER the routing.sh activation stage (so routing remains the last runtime activation), AND mkdir runs as Stage 5:
      `grep -nE 'Creating splitgate namespace|Activating routes via routing\.sh|Deploying splitgate dispatcher|Deploying logrotate config' deploy.sh` shows them in order: Creating splitgate namespace (Stage 5), Activating routes via routing.sh (Stage 25), Deploying splitgate dispatcher (Stage 26), Deploying logrotate config (Stage 27).
    - `grep -c 'echo "\[5/\${TOTAL_STAGES}\] Creating splitgate namespace' deploy.sh` is exactly 1.
    - `grep -c 'echo "\[26/\${TOTAL_STAGES}\] Deploying splitgate dispatcher' deploy.sh` is exactly 1.
    - `grep -c 'echo "\[27/\${TOTAL_STAGES}\] Deploying logrotate config' deploy.sh` is exactly 1.
    - Final summary block contains both new PHASE 10 bullets: `grep -c 'PHASE 10:' deploy.sh` is at least 2.
    - Final summary banner mentions "Phase 10": `grep -c 'Phase 10' deploy.sh` is at least 2 (banner + verification heading).
    - `grep -E 'sudo /etc/(routing\.sh|update-vpn-routes|vpn-status\.sh|vpn-rollback\.sh)' deploy.sh | wc -l | tr -d ' '` returns `0` (no stale literal old paths anywhere in the file).
    - Stage 21b header bug fixed: `grep -c '\[22b/' deploy.sh` is exactly 1, and `grep -c '\[21/.*Deploying ru-exclude' deploy.sh` is 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS deploy host → RPi via SSH | deploy.sh issues `sudo` on the RPi to write under /etc/ and /usr/local/bin/ |
| operator → deploy.sh argv | `--no-run` flag is the only argument; no untrusted argv values reach ssh strings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | new file SCP staging in /tmp | mitigate | All new stages use the existing scp→`/tmp/foo.tmp`→`sudo mv ${TMP} ${REMOTE}` pattern; same trust model as Stages 22 and 23 |
| T-10-05 | Elevation of Privilege | `chmod +x /usr/local/bin/splitgate` | accept | dispatcher is root-owned and only execs root-owned scripts under `/etc/splitgate/` via sudo; identical risk surface to existing `chmod +x /etc/vpn-status.sh` (Stage 19) |
| T-10-06 | Denial of Service | TOTAL_STAGES mismatch causing broken progress display | mitigate | Task 2 enforces grep-based stage-count gate; `[N/${TOTAL_STAGES}]` headers all updated; Pitfall 6 fix included |
| T-10-SC | Tampering | No new package installs | accept | Phase 10 adds no npm/pip/cargo or apt installs (logrotate is pre-installed per RESEARCH A1); no Package Legitimacy Audit required |
</threat_model>

<verification>
- `bash -n deploy.sh` exits 0 — syntax is valid.
- The unchanged paths (D-06..D-12) still appear in their `*_REMOTE` declarations: `/etc/amnezia/amneziawg/awg0.conf`, `/etc/cron.d/vpn-routes`, `/etc/dnsmasq.conf`, `/etc/NetworkManager/dispatcher.d/10-vpn-routes`, `/etc/systemd/system/vpn-routing.service`.
- The new Stage 5 (mkdir) precedes every file-deploy into the splitgate namespace (verified by line-order awk).
- Routing.sh activation remains the last runtime activation stage (Stage 25 after renumber) before the two new "deploy artifact" stages 26 and 27 (D-17, D-19).
- No literal old `/etc/routing.sh`, `/etc/vpn-status.sh`, `/etc/vpn-rollback.sh`, `/etc/update-vpn-routes`, `/etc/asn-lookup.py`, or `/etc/white-list-extended.txt` remain anywhere in deploy.sh.
</verification>

<success_criteria>
1. `TOTAL_STAGES=27` and `bash -n deploy.sh` exits 0.
2. All nine migrated `*_REMOTE` variables point under `/etc/splitgate/`.
3. Eight new SPLITGATE_/LOGROTATE_ variables declared with the exact values listed.
4. Three new stages (mkdir, dispatcher, logrotate) inserted with correct ordering and chmod/chown.
5. Stage 21b header bug from Pitfall 6 corrected.
6. Final summary echoes new PHASE 10 bullets and Phase 10 verification block.
</success_criteria>

<output>
Create `.planning/phases/10-splitgate-ergonomics/10-02-SUMMARY.md` when done.
</output>
