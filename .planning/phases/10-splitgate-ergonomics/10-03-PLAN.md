---
phase: 10-splitgate-ergonomics
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - src/src/scripts/splitgate
  - src/src/configs/logrotate-vpn-gateway
  - README.md
  - docs/README.ru.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "src/src/scripts/splitgate exists, is executable, and dispatches all five subcommands (status, watch, rollback, routing, update) to /etc/splitgate/ targets via exec (D-13, D-14, D-15)"
    - "Unknown or empty subcommand prints `Usage: splitgate {status|watch|rollback|routing|update} [args...]` to stdout and exits 1 (D-16)"
    - "src/src/configs/logrotate-vpn-gateway exists with the /etc/splitgate/logs/vpn-gateway.log target, daily rotation, rotate 14, compress, dateext, missingok, notifempty, create 0640 root root (D-02, D-19)"
    - "README.md and docs/README.ru.md document the splitgate CLI (the five subcommands), the new /etc/splitgate/ filesystem layout, and the updated rollback behavior (D-18: removes whole /etc/splitgate/ tree)"
  artifacts:
    - path: "src/src/scripts/splitgate"
      provides: "ergonomic CLI dispatcher to /etc/splitgate/* tools"
      exports: ["status", "watch", "rollback", "routing", "update"]
      min_lines: 10
    - path: "src/src/configs/logrotate-vpn-gateway"
      provides: "logrotate stanza for /etc/splitgate/logs/vpn-gateway.log"
      contains: "/etc/splitgate/logs/vpn-gateway.log"
    - path: "README.md"
      provides: "Operator-facing English docs reflecting Phase 10 changes"
      contains: "splitgate"
    - path: "docs/README.ru.md"
      provides: "Operator-facing Russian docs reflecting Phase 10 changes"
      contains: "splitgate"
  key_links:
    - from: "src/src/scripts/splitgate"
      to: "/etc/splitgate/vpn-status.sh, /etc/splitgate/watch-routes.py, /etc/splitgate/vpn-rollback.sh, /etc/splitgate/routing.sh, /etc/splitgate/update-vpn-routes"
      via: "exec sudo dispatch in case statement"
      pattern: "exec sudo \\$SPLITGATE_DIR/"
    - from: "src/src/configs/logrotate-vpn-gateway"
      to: "/etc/splitgate/logs/vpn-gateway.log"
      via: "logrotate stanza filename"
      pattern: "/etc/splitgate/logs/vpn-gateway.log \\{"
---

<objective>
Create the two new repository artifacts that Phase 10 introduces: `src/src/scripts/splitgate` (the dispatcher CLI deployed to `/usr/local/bin/splitgate`) and `src/src/configs/logrotate-vpn-gateway` (the logrotate stanza deployed to `/etc/logrotate.d/vpn-gateway`). Both files do not exist yet in the repo — Plan 02 already declares the `*_LOCAL` variables and adds preflight existence checks pointing at these filenames, so Plan 02's preflight will fail until this plan completes. After creating these two files, update `README.md` and `docs/README.ru.md` to document the splitgate CLI, the new `/etc/splitgate/` filesystem layout on the RPi, and the updated rollback behavior (D-18 removes the entire splitgate tree).

Purpose: Without these files Phase 10 deploy cannot complete preflight; without docs updates the project violates the CLAUDE.md convention that README files stay in sync with shipped changes.

Output: src/src/scripts/splitgate (chmod +x in the repo), src/src/configs/logrotate-vpn-gateway, updated README.md, updated docs/README.ru.md.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/10-splitgate-ergonomics/10-CONTEXT.md
@.planning/phases/10-splitgate-ergonomics/10-RESEARCH.md

<!-- The exact dispatcher template is in 10-CONTEXT.md under <code_context>, and
     repeated verbatim in 10-RESEARCH.md "New Files to Create" → "src/scripts/splitgate".
     The logrotate stanza is in 10-RESEARCH.md "New Files to Create" →
     "src/src/configs/logrotate-vpn-gateway". -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/src/scripts/splitgate dispatcher</name>
  <files>src/src/scripts/splitgate</files>
  <read_first>
    - .planning/phases/10-splitgate-ergonomics/10-CONTEXT.md — D-13 (path is `/usr/local/bin/splitgate`, bash), D-14 (subcommand→target map for all five subcommands), D-15 (args pass-through), D-16 (unknown subcommand prints exact `Usage:` string and exits 1), D-17 (deploy stage will chmod +x, root:root). The `<code_context>` block contains the verbatim dispatcher template.
    - .planning/phases/10-splitgate-ergonomics/10-RESEARCH.md — section "New Files to Create" → "src/src/scripts/splitgate (dispatcher)" reproduces the same template and explains why `watch` uses `sudo python3` while the others use `exec sudo`.
    - src/scripts/install-awg.sh — existing repo convention for bash shebang + script header comments + `set -e` style (read just the first 30 lines to match the style for the new file's preamble).
  </read_first>
  <action>
    Create `src/scripts/splitgate` as a bash script implementing the exact dispatcher specified by D-13..D-16. Required structure (do not invent variations — match the canonical template):

    - Shebang: `#!/bin/bash`
    - Header comment block matching the project convention (single-line `# src/src/scripts/splitgate — ergonomic dispatcher for /etc/splitgate tools`, Deployed-to line, Decisions block listing D-13..D-18). Keep the header concise (under 20 lines).
    - Constant: `SPLITGATE_DIR="/etc/splitgate"`
    - Read first positional argument with safe defaulting: `CMD="${1:-}"` then `shift || true`
    - `case "$CMD" in` with FIVE branches in this exact subcommand→target mapping (per D-14):
      * `status)   exec sudo "$SPLITGATE_DIR/vpn-status.sh" "$@" ;;`
      * `watch)    exec sudo python3 "$SPLITGATE_DIR/watch-routes.py" "$@" ;;`
      * `rollback) exec sudo "$SPLITGATE_DIR/vpn-rollback.sh" "$@" ;;`
      * `routing)  exec sudo "$SPLITGATE_DIR/routing.sh" "$@" ;;`
      * `update)   exec sudo "$SPLITGATE_DIR/update-vpn-routes" "$@" ;;`
    - Default branch `*)` prints (to stdout) the EXACT string `Usage: splitgate {status|watch|rollback|routing|update} [args...]` and then `exit 1`. The Usage string is verbatim — do not paraphrase, do not add ANSI colors, do not reorder subcommands (D-16).
    - Close the case block with `esac`.

    Set the file mode to `0755` in the repo (`chmod +x src/src/scripts/splitgate`) so the repo file is executable. The deploy stage in deploy.sh Stage 26 (added in Plan 02 Task 2) will re-apply chmod +x on the RPi after `sudo mv`.

    The file MUST NOT add any extra subcommands beyond the five listed (D-CONTEXT "Out of scope: New subcommands beyond the 5 defined"). The file MUST NOT add logging, telemetry, env-sourcing, or sanity checks beyond what the template specifies — keep it a minimal dispatcher.

    Do NOT use `set -euo pipefail` — `exec` replaces the shell process; defensive flags are unnecessary and could mask the subcommand's own exit code semantics. (Matches the template in CONTEXT.md.)
  </action>
  <verify>
    <automated>bash -n src/src/scripts/splitgate && [ -x src/src/scripts/splitgate ] && bash src/src/scripts/splitgate 2>&1 | grep -qF 'Usage: splitgate {status|watch|rollback|routing|update} [args...]' && bash src/src/scripts/splitgate; [ $? -eq 1 ]</automated>
  </verify>
  <done>
    - `bash -n src/src/scripts/splitgate` exits 0.
    - `[ -x src/src/scripts/splitgate ]` is true (file mode includes +x).
    - `bash src/src/scripts/splitgate` (no args) writes the exact literal `Usage: splitgate {status|watch|rollback|routing|update} [args...]` to stdout and exits with code `1`.
    - `bash src/src/scripts/splitgate not-a-command` does the same.
    - `grep -c '^SPLITGATE_DIR="/etc/splitgate"$' src/src/scripts/splitgate` is exactly `1`.
    - `grep -cE '^\s*(status|watch|rollback|routing|update)\)' src/src/scripts/splitgate` is exactly `5` (all five subcommands present, each as its own case branch).
    - `grep -c 'exec sudo' src/src/scripts/splitgate` is at least `4` (status, rollback, routing, update use `exec sudo $path`; watch uses `exec sudo python3 $path`).
    - `grep -c 'exec sudo python3' src/src/scripts/splitgate` is exactly `1` (watch branch only).
    - File line count is under 30: `wc -l src/src/scripts/splitgate` ≤ 30 (sanity bound on the "minimal dispatcher" rule).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/src/configs/logrotate-vpn-gateway</name>
  <files>src/src/configs/logrotate-vpn-gateway</files>
  <read_first>
    - .planning/phases/10-splitgate-ergonomics/10-CONTEXT.md — D-02 (logs subdirectory `/etc/splitgate/logs/`), D-05 (`/etc/splitgate/logs/vpn-gateway.log`), D-11 (logrotate config file location stays at `/etc/logrotate.d/vpn-gateway` system path), D-19 (Phase 9 D-01 `/var/log/vpn-gateway.log` is SUPERSEDED — DO NOT use that path).
    - .planning/phases/10-splitgate-ergonomics/10-RESEARCH.md — section "New Files to Create" → "src/src/configs/logrotate-vpn-gateway (Phase 9 override)" gives the canonical stanza text.
    - .planning/phases/09-operational-logging/09-CONTEXT.md — Phase 9's intended log function pattern. Phase 10 only ships the rotation config (not the log() function rewrite). The config must reference the NEW splitgate path, not Phase 9's superseded `/var/log/vpn-gateway.log`.
  </read_first>
  <action>
    Create `src/src/configs/logrotate-vpn-gateway` with the canonical stanza specified in 10-RESEARCH.md "New Files to Create" → "src/src/configs/logrotate-vpn-gateway":

    - First line: `/etc/splitgate/logs/vpn-gateway.log {` (the target log file path is the splitgate-namespaced one per D-02/D-05/D-19, NOT `/var/log/vpn-gateway.log`)
    - Body directives, one per line, indented with 4 spaces:
      * `daily`
      * `rotate 14`
      * `compress`
      * `dateext`
      * `missingok`
      * `notifempty`
      * `create 0640 root root`
    - Closing line: `}`

    No additional directives (no `postrotate`, no `sharedscripts`, no `delaycompress`, no `copytruncate` — none are in the canonical stanza and adding any of them changes rotation semantics that Phase 9 has not requested).

    File mode: 644 (default for tracked regular files in this repo). No shebang — logrotate config files are not executables.

    Header comment (single line at the top, starting with `#`) is acceptable but optional. If included, use a single line: `# src/configs/logrotate-vpn-gateway — deployed to /etc/logrotate.d/vpn-gateway (D-11, D-19). Phase 10.`
  </action>
  <verify>
    <automated>test -f src/configs/logrotate-vpn-gateway && grep -q '^/etc/splitgate/logs/vpn-gateway.log {$' src/configs/logrotate-vpn-gateway && grep -qE '^\s+daily$' src/configs/logrotate-vpn-gateway && grep -qE '^\s+rotate 14$' src/configs/logrotate-vpn-gateway && grep -qE '^\s+create 0640 root root$' src/configs/logrotate-vpn-gateway && grep -q '^}$' src/configs/logrotate-vpn-gateway && ! grep -q '/var/log/vpn-gateway.log' src/configs/logrotate-vpn-gateway</automated>
  </verify>
  <done>
    - File `src/configs/logrotate-vpn-gateway` exists.
    - `grep -c '^/etc/splitgate/logs/vpn-gateway.log {$' src/configs/logrotate-vpn-gateway` is exactly `1`.
    - Each directive present exactly once and exactly as named: `daily`, `rotate 14`, `compress`, `dateext`, `missingok`, `notifempty`, `create 0640 root root`. Verified by: `for d in daily 'rotate 14' compress dateext missingok notifempty 'create 0640 root root'; do grep -cqE "^[[:space:]]+${d}\$" src/configs/logrotate-vpn-gateway || exit 1; done`
    - Closing `}` on its own line.
    - `grep -c '/var/log/vpn-gateway.log' src/configs/logrotate-vpn-gateway` is `0` (Phase 9 D-01 path absent — D-19 enforced).
    - No unintended directives: `grep -cE '^[[:space:]]+(postrotate|prerotate|sharedscripts|delaycompress|copytruncate|olddir|monthly|weekly)' src/configs/logrotate-vpn-gateway` is `0`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update README.md and docs/README.ru.md for splitgate CLI + namespace + rollback behavior</name>
  <files>README.md, docs/README.ru.md</files>
  <read_first>
    - README.md — the entire current file. Pay attention to: (a) the CLI reference section that lists the six on-RPi scripts with their `/etc/...` paths and example invocations; (b) the rollback section that describes which files are preserved; (c) the file-layout/filesystem section if one exists.
    - docs/README.ru.md — the entire current file. The Russian doc mirrors README.md section-for-section.
    - .planning/phases/10-splitgate-ergonomics/10-CONTEXT.md — D-13..D-19 (everything operator-facing).
    - .planning/phases/10-splitgate-ergonomics/10-RESEARCH.md — section "Recommended Project Structure (after Phase 10)" gives the final RPi filesystem tree to document.
    - CLAUDE.md — convention "After completing any task, review README.md and docs/README.ru.md... Keep both files in sync with each other."
  </read_first>
  <action>
    Update both README.md and docs/README.ru.md so they document the Phase 10 changes. Apply identical structural edits to both files; translate the new English text into Russian for docs/README.ru.md (use the existing Russian terminology in the file for consistency — do not introduce new translations for words already used).

    Changes to make in BOTH README.md and docs/README.ru.md:

    1. Add a new section titled "splitgate CLI" (RU: "splitgate CLI" or matching the file's section-heading style). Place it BEFORE the existing CLI reference for individual scripts (or as a subsection at the top of that area). Content:
       - One sentence: `splitgate` is the ergonomic dispatcher installed at `/usr/local/bin/splitgate` that runs the five core tools without typing their full paths.
       - A table or definition list with five rows:
         | Command | Runs |
         | `splitgate status [args...]` | `sudo /etc/splitgate/vpn-status.sh [args...]` |
         | `splitgate watch [args...]` | `sudo python3 /etc/splitgate/watch-routes.py [args...]` |
         | `splitgate rollback [args...]` | `sudo /etc/splitgate/vpn-rollback.sh [args...]` |
         | `splitgate routing [args...]` | `sudo /etc/splitgate/routing.sh [args...]` |
         | `splitgate update [args...]` | `sudo /etc/splitgate/update-vpn-routes [args...]` |
       - A note: running `splitgate` with no arguments (or an unknown subcommand) prints `Usage: splitgate {status|watch|rollback|routing|update} [args...]` and exits 1.

    2. Update the existing CLI reference section so every previously-documented invocation of `sudo /etc/routing.sh`, `sudo /etc/vpn-status.sh`, `sudo /etc/vpn-rollback.sh`, `sudo /etc/update-vpn-routes`, `python3 /etc/watch-routes.py`, `python3 /etc/asn-lookup.py` is updated to its new `/etc/splitgate/...` path. Where the existing doc uses the old path inside a code fence, change the path in-place; do not delete the example.

    3. Add (or update if a "Filesystem layout on the RPi" section exists) a short tree-style block showing the new layout:

       ```
       /etc/splitgate/
       ├── routing.sh
       ├── vpn-rollback.sh
       ├── vpn-status.sh
       ├── update-vpn-routes
       ├── watch-routes.py
       ├── asn-lookup.py
       ├── vpn-gateway.env
       ├── white-list.txt          (generated at runtime)
       ├── white-list-extended.txt (optional)
       ├── ru-exclude.txt          (optional)
       └── logs/                   (created at deploy; Phase 9 writes vpn-gateway.log here)

       /usr/local/bin/splitgate    (dispatcher CLI)
       /etc/logrotate.d/vpn-gateway (rotates /etc/splitgate/logs/vpn-gateway.log)
       ```

       Files that intentionally stay at system locations are listed as a follow-up paragraph: `/etc/systemd/system/vpn-routing.service`, `/etc/cron.d/vpn-routes`, `/etc/dnsmasq.conf`, `/etc/NetworkManager/dispatcher.d/10-vpn-routes`, `/etc/iptables/rules.v4`, `/etc/amnezia/amneziawg/awg0.conf` — each required by its consuming daemon.

    4. Update the Rollback section to reflect D-18 behavior change: `sudo /etc/splitgate/vpn-rollback.sh` (or via the dispatcher: `sudo splitgate rollback`) now removes `/usr/local/bin/splitgate` AND the entire `/etc/splitgate/` tree, INCLUDING `white-list.txt` (which was previously preserved). State this explicitly so operators know runtime-generated state is reset. Files still preserved: AmneziaWG packages and `/etc/amnezia/amneziawg/awg0.conf`.

    5. If a "Phase status" or "Recent changes" / changelog section exists in either README, add a Phase 10 entry: "Phase 10 — Splitgate ergonomics: consolidated all RPi files under `/etc/splitgate/`, added `splitgate` dispatcher CLI, added log rotation for `/etc/splitgate/logs/vpn-gateway.log`."

    Both files MUST stay synchronized — every English section change has a Russian equivalent in docs/README.ru.md and vice versa. The dispatcher invocation table columns and the filesystem tree are language-neutral (paths and commands stay verbatim); only headings and prose narrative get translated.

    Do NOT introduce documentation for Phase 9 work (`log()` function rewrite, journal integration). Phase 9 is still pending; this plan only documents the artifacts Phase 10 itself ships.
  </action>
  <verify>
    <automated>grep -q 'splitgate status' README.md && grep -q 'splitgate watch' README.md && grep -q '/etc/splitgate/' README.md && grep -q 'splitgate status' docs/README.ru.md && grep -q '/etc/splitgate/' docs/README.ru.md && ! grep -E 'sudo /etc/(routing\.sh|vpn-status\.sh|vpn-rollback\.sh|update-vpn-routes|asn-lookup\.py|watch-routes\.py)\b' README.md && ! grep -E 'sudo /etc/(routing\.sh|vpn-status\.sh|vpn-rollback\.sh|update-vpn-routes|asn-lookup\.py|watch-routes\.py)\b' docs/README.ru.md</automated>
  </verify>
  <done>
    - Both README.md and docs/README.ru.md mention `splitgate status`, `splitgate watch`, `splitgate rollback`, `splitgate routing`, `splitgate update` at least once each.
    - The five dispatcher subcommands → target paths are documented in BOTH files: `grep -c '/etc/splitgate/vpn-status.sh' README.md` ≥ 1; same for `/etc/splitgate/watch-routes.py`, `/etc/splitgate/vpn-rollback.sh`, `/etc/splitgate/routing.sh`, `/etc/splitgate/update-vpn-routes`. Same checks on docs/README.ru.md.
    - No stale `sudo /etc/{routing,vpn-status,vpn-rollback,update-vpn-routes,asn-lookup,watch-routes}` invocation literals remain (the grep gate in `<verify>` returns 0 matches).
    - Filesystem layout block appears in both files, containing the literal substring `/etc/splitgate/` and `/usr/local/bin/splitgate`.
    - Rollback section in both files explicitly mentions that the entire `/etc/splitgate/` tree (including `white-list.txt`) is removed by rollback (D-18 behavior change).
    - The Usage string is mentioned at least once in each file: `grep -Fc 'Usage: splitgate {status|watch|rollback|routing|update} [args...]' README.md` ≥ 1 and the same on docs/README.ru.md.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → splitgate dispatcher argv | user-supplied args after subcommand are forwarded verbatim via `"$@"` |
| splitgate dispatcher → target scripts | targets already validate their own argv (vpn-status.sh has strict --via/--filter validation per Phase 5 D-10/D-11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Tampering | dispatcher dispatching unintended target | mitigate | `case` statement uses literal string match (no glob/regex); unknown subcommand routes to default `*)` branch; the case branches use a fixed `$SPLITGATE_DIR` constant (not user-influenced) so target path cannot be redirected by argv |
| T-10-08 | Elevation of Privilege | sudo wrapper allowing escalation via subcommand | accept | Five target paths are root-owned scripts that were already invoked as `sudo /etc/...` by operators; dispatcher does not widen the attack surface |
| T-10-09 | Information Disclosure | logrotate `create 0640 root root` permissions | mitigate | mode `0640` keeps the rotated log readable only by root; matches Phase 9 D-01 intent |
| T-10-SC | Tampering | No new package installs | accept | src/scripts/splitgate is a hand-rolled bash file; src/configs/logrotate-vpn-gateway is a hand-rolled config; no third-party dependencies introduced |
</threat_model>

<verification>
- `bash -n src/scripts/splitgate` exits 0 and `[ -x src/scripts/splitgate ]` is true.
- `bash src/scripts/splitgate` (no args) prints the exact Usage string and exits with code 1.
- `bash src/scripts/splitgate <unknown>` does the same.
- `grep -cE '^\s*(status|watch|rollback|routing|update)\)' src/scripts/splitgate` is exactly 5.
- src/configs/logrotate-vpn-gateway opens with `/etc/splitgate/logs/vpn-gateway.log {` and contains the seven directives once each.
- Both README files document the splitgate CLI, the new filesystem layout, and the D-18 rollback behavior change; neither file contains stale `sudo /etc/` literals for any of the six migrated scripts.
- `bash -n` succeeds against the entire updated scripts/ tree from Plan 01 (transitive sanity).
</verification>

<success_criteria>
1. src/scripts/splitgate exists, is executable, and behaves exactly per D-13..D-16 (five subcommands dispatch via exec; default branch prints exact Usage string and exits 1).
2. src/configs/logrotate-vpn-gateway exists with the canonical stanza targeting `/etc/splitgate/logs/vpn-gateway.log`.
3. README.md and docs/README.ru.md document the splitgate CLI (all five subcommands), the new filesystem layout, and the updated rollback behavior — both files in sync.
4. No stale `sudo /etc/<script>` invocation strings remain in either README file for the six migrated scripts.
5. deploy.sh preflight (set up by Plan 02 Task 1) succeeds because both new local files now exist.
</success_criteria>

<output>
Create `.planning/phases/10-splitgate-ergonomics/10-03-SUMMARY.md` when done.
</output>
