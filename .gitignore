/.idea/
/.claude/

# Phase 1: VPN secrets — never commit
.env.secrets
/awg0.conf

# Phase 5: user-defined ISP-bypass exceptions — never commit
src/configs/white-list-extended.txt
# Phase 8: operator CIDR exclusion list — never commit
src/configs/ru-exclude.txt

# Python bytecode cache
__pycache__/
*.pyc

logs
